# --- Rust / Cargo ---
/target
**/target
**/*.rs.bk
**/Cargo.lock.bak
# Cargo.lock do workspace raiz DEVE ser versionado (workspace com binário;
# garante build reprodutível do instalador). Libs puras ignorariam, mas
# temos apps/desktop/src-tauri, então versiona.

# --- Node / Vite / npm ---
node_modules/
**/node_modules/
dist/
**/dist/
.npm/
.yarn/
.pnpm-store/
.pnp.*

# --- Tauri ---
apps/desktop/src-tauri/gen/
apps/desktop/src-tauri/target/

# --- Bancos de dados de runtime ---
*.db
*.db-journal
*.db-shm
*.db-wal
storage/test-dbs/

# --- Logs / cobertura ---
*.log
# Feedback da Etapa 2A (Fase 5): `cargo test ... > test.log 2>&1`
# + `tail -80 test.log` para todo teste de worker. Listado
# explicitamente por clareza, embora `*.log` já cubra.
test.log
coverage/
**/coverage/

# --- IDE / editores ---
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db
desktop.ini

# --- OneDrive (artefatos locais) ---
*.tmp
*.bak
# Classe recorrente de subproduto de teste que escapa pro FS local
# (não pro tempdir do sistema). Já aconteceu com:
#   - `real_minimal.docx` em workers/document-worker/ (PR #24, antes
#     do bump atomico da Etapa 5.X escrever dentro do jail)
#   - outros `tmp_<nonce>/` que helpers de teste podem criar
#   - `tmp-<descricao>.ps1` que scripts de sessão PowerShell criam
#     (Etapa 1 da Fase 6: `tmp-run-gate.ps1` para rodar o gate
#     antes do push — fifth occurrence of "subproduto na arvore")
# Padrão profilático — quando aparecer um novo tmp[-_]*, a regra
# **nao** deve ser "adiciona entrada especifica pro nome"; a regra
# e' consertar a origem (teste deve escrever em tempdir) e deixar
# o `tmp[-_]*` apanhar. O comentario do commit de docs da Etapa 5.X
# detalha. As duas grafias (hifen e underscore) sao cobertas porque
# a grafia que escapa do `tmp_*` e' `tmp-*` e vice-versa.
tmp_*
tmp-*
*_tmp/

# --- Variáveis de ambiente ---
.env
.env.local
.env.*.local
**/.env
**/.env.local

# --- Python (vai entrar na Fase 5, mas já preparado) ---
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/

# --- Sistema de arquivos do Windows ---
$RECYCLE.BIN/

# --- Builds do instalador (gerados pelo tauri build) ---
apps/desktop/src-tauri/target/release/bundle/

# --- Trash local (arquivos movidos pra fora do repo durante
#     debugging, antes do mavis-trash ficar disponível) ---
.trash/
.trash/**
