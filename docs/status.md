# Estado Real por Fase

Primeiro arquivo a ser lido por qualquer sessão nova de IA, **depois de** `REGRAS-DO-PROJETO.md`.

## Estados possíveis

- `não iniciada` — fase planejada, nenhum trabalho começou.
- `em andamento` — código está sendo escrito; testes da fase ainda não todos verdes.
- `concluída` — todos os testes da fase passam; specs correspondentes promovidos a `parcialmente implementado` ou `implementado`; changelog atualizado.
- `bloqueada` — algo impede progredir; motivo documentado na coluna "Pendências".

## Regra de promoção

Promover uma fase de `em andamento` para `concluída` exige, simultaneamente:

1. Suíte de testes da fase 100% verde.
2. Specs correspondentes com `Estado` atualizado para `parcialmente implementado` ou `implementado`, com carimbo de verificação recente.
3. Entrada em `CHANGELOG.md` descrevendo o efeito para o usuário.
4. Referência ao PR / commit que consolidou a fase.

## Tabela

| Fase | Nome | Estado | Evidência | Pendências |
|------|------|--------|-----------|------------|
| 0 | Fundação documental | concluída | este `status.md`; PR de fundação documental; 9 specs em `docs/architecture/`; 4 ADRs em `docs/decisions/`; `REGRAS-DO-PROJETO.md` com §1.13 | — |
| 1 | Fundação (Tauri + Rust + SQLite) | concluída | suíte workspace 15/15 verde (`cargo test`); `cargo clippy --workspace -- -D warnings` limpo; `npm run build` verde; `scripts/check-core-purity.ps1` OK; `cargo tauri build` produz `target/release/bundle/nsis/Frederico IA Studio_0.1.0_x64-setup.exe` (3,0 MB) | — |
| 2 | Chat e provedores | concluída | Etapas 1, 2, Leva 3 e Etapa 5 (UI) fechadas; **Hardening 1 (DPAPI real) fechado**: `WindowsCredentialStore` implementado via `CredWriteW`/`CredReadW`/`CredDeleteW`/`CredEnumerateW` (crate `windows` v0.58), `TargetName` `Frederico-IA-Studio:provider:<id>`, mapeamento correto de HRESULT→win32 (`& 0xFFFF`), `CredFree` chamado uma única vez por alocação (evitando double-free); 6 testes de integração (`tests/windows_credential_store.rs`) com mutex global de serialização e IDs únicos por run: set/get roundtrip, get-nonexistent→None, delete idempotente, list filtra prefixo Frederico, list filtra prefixo + valida que credencial de outro app não vaza, overwrite de credencial existente. Casca Tauri: `AppState.credentials: Arc<WindowsCredentialStore>` único no processo; `ProviderSetCredential`/`ProviderDeleteCredential` agora chamam DPAPI real (não mais `FakeCredentialStore` ad-hoc). `Cargo.toml` de `frederico-security`: `unsafe_code = "deny"` (era `forbid`); `#![allow(unsafe_code)]` apenas no módulo `windows.rs` e nos tests. `scripts/check-core-purity.ps1` reconhece `crates/security/src/windows*` e `crates/security/tests*` como exceções legítimas. **Hardening 3 (`provider-recorder`) fechado**: novo binário `recorder` em `crates/provider-engine/src/bin/recorder.rs` que faz chamada real a um provedor OpenAI-compat (OpenAI/OpenRouter/DeepSeek/Mistral/NVIDIA NIM/Ollama/LM Studio), captura os bytes brutos do stream SSE e grava em `fixtures/<provider>/<scenario>.jsonl` com header `# recorded_at=... provider=... model=... base_url=... format=openai-compat-sse`. **Sanitização obrigatória**: módulo `frederico_provider_engine::sanitize` (regex case-insensitive bloqueando `Authorization`/`api_key`/`x-api-key`/`Bearer `/`sk-`/`sk-ant-`/`gsk_`/`or-`); o recorder chama `sanitize::check` antes do flush e, se falhar, **deleta o arquivo** e aborta com exit 1. Integration test `tests/fixtures_sanitize.rs` varre `fixtures/**/*.jsonl` no CI e quebra o build se algum estiver contaminado. Leitura da chave via `--api-key-env VAR` (default `OPENAI_API_KEY`) — nunca via `CredentialStore` (a chave do recorder é descartável, do CI/dev, não do app). **Hardening 4 (contract tests off-PR) fechado**: 3 integration tests em `crates/provider-engine/tests/openai_compat_contract.rs` (OpenAI + OpenRouter) e `crates/provider-engine/tests/anthropic_contract.rs` (Anthropic). Cada um gateado em env var (`OPENAI_API_KEY`/`OPENROUTER_API_KEY`/`ANTHROPIC_API_KEY`); se ausente, o test **pula** com mensagem clara em vez de falhar — assim a suíte roda limpa no PR (sem chave) e roda de verdade no CI noturno (com chave). Os tests validam que o adapter produz pelo menos 1 `Delta` e exatamente 1 `Done` com `StopReason` válido, contra a API real. Helper compartilhado em `tests/common/mod.rs` (`require_env`, `openai_adapter_or_skip`, `drain_events`). Runner dedicado: `scripts/run-contract-tests.ps1` (mostra quais env vars estão setadas, mascarando a chave; roda `cargo test --test openai_compat_contract --test anthropic_contract`). **Hardening 5 (recovery E2E) fechado**: 3 integration tests em `crates/provider-engine/tests/recovery.rs` que validam a regra do **"Journal de eventos"** — o SQLite é a fonte de verdade, sobrevive a drop do orquestrador. `journal_persists_events_across_orchestrator_drop` dispara run com 5 deltas + Done, espera completar, dropar orquestrador A, reabre db com orquestrador B, valida que `MessageEventRepo::list_for_message` retorna os mesmos 5 deltas + Done. `cancel_idempotent_and_status_persists` valida que `cancel_run` é idempotente e o status final (Completed/Cancelled/etc.) bate entre sink e db após recovery. `user_message_persisted_before_run_starts` valida que a `Message.role=user` está no db imediatamente após `send_message` retornar (regra do "Journal-then-emit" aplicada também à user msg). Suíte workspace **133/133 verde** (era 130/130; +3 do recovery); `cargo clippy --workspace --all-targets -- -D warnings` limpo; `check-core-purity.ps1` verde; `tsc --noEmit` + `npm run build` verdes | — |
| 3 | Motor de execução e ferramentas | concluída | **Etapas 1, 2, 3, 4, 4.x, 4.x.y, 5, 5.x, 4.1, 6.1 e 6.2 fechadas (Fase 3 completa).** **Etapa 4** (RunExecutor fecha o loop tool_call — "Fluxo vertical 1"): `frederico-execution-engine` no workspace (`unsafe_code = "forbid"`). `RunExecutor` consome `BoxStream<StreamEvent>`, valida via `validate_tool_call` (10 passos), executa via `Tool::execute`, emite `StreamEvent::ToolResult { id, ok, output }` (persistido em `message_events` com `kind = 'tool_result'`), adiciona ao contexto via `ChatMessage::tool(name, content, tool_call_id)` e volta a chamar o modelo. Loop até `Done`/`Error`/`Cancelled` ou budget estourado. `BudgetEnforcer` aplica `max_steps`. Mudanças no `provider-engine`: `Role::Tool` + `tool_call_id`. Migração `0004_tool_result_kind.sql`. **Etapa 4.x** (cancelamento + recovery): `CancellationToken` checado em 3 pontos do loop (entre rounds, entre eventos, e no `StreamEvent::Cancelled`). 2 testes em `tests/cancel.rs` (`cancel_via_provider_cancelled_event`, `cancel_via_token_before_first_tick`); 4 testes em `tests/recovery.rs` (replicam "Journal-then-emit"); `tests/common/mod.rs` extraído. **Etapa 4.x.y** (reorganizar crates — `ChatOrchestrator` movido pro `execution-engine` que delega o loop pro `RunExecutor`): resolve o ciclo de dependência (`provider-engine` re-exportava o `ChatOrchestrator` que dependia do `RunExecutor` que dependia do `provider-engine`). Grafo agora é `provider-engine ──► execution-engine ──► provider-engine` (sem ciclo). `provider-engine::orchestrator` mantém só `error_to_view` + `ProviderErrorView` + `OrchestratorError`/`OrchestratorResult` definidos **localmente**. `ChatOrchestrator::new` ganhou 4 args novos (`tool_registry`, `jail`, `tools`, `allowed_for_run`). `send_message` agora delega o loop pro `RunExecutor` via `tokio::spawn`. Cálculo de custo real reintroduzido via `descriptor.cost_microcents(p, c)` + `MessageRepo::set_usage_and_cost(p, c, cost)` + `ConversationRepo::add_cost(conv_id, cost)` quando `Completed`. 5 tests migraram do `provider-engine::orchestrator::tests` pro `execution-engine::tests::integration_orchestrator.rs`. Casca Tauri constrói o `ChatOrchestrator` com o tooling inicial (`ToolRegistry` + `Jail` no `current_dir` + `FilesReadTool` + `[ToolId::new("files.read")]`). **Etapa 5** (watchdog de 60s — escopo mínimo): `tokio::select!` no loop interno com 3 braços (`cancel.cancelled()` biased, `tokio::time::sleep(event_timeout)` watchdog, `stream.next()`). `event_timeout` configurável via `RunExecutor::with_event_timeout(Duration)` (encadeável; default 60s). Watchdog estoura → `MessageStatus::Timeout` / `RunStatus::Timeout` / `RunState::Interrupted` (a view `runs_with_status` mapeia `interrupted → timeout`). 1 teste em `tests/watchdog.rs` com `TrailingAdapter` que emite 1 delta e trava. **Etapa 5.x** (cancel real do HTTP + recovery de crash + `BEGIN IMMEDIATE` + state granular + Passo 10 auditoria — tudo numa única sub-etapa): **5.x.1 — Cancel real do HTTP**: o `tokio::select!` com `cancel.cancelled()` no `OpenAiCompatAdapter::stream` (e `AnthropicAdapter::stream`) JÁ EXISTIA desde antes; o teste E2E em `crates/provider-engine/tests/cancel_http.rs` (`cancel_drops_reqwest_connection` + `stream_completes_normally_when_not_cancelled`) prova agora que a stack é full-stack — o cancel fecha a conexão TCP, o reqwest aborta, o server detecta o EOF. 2 tests novos verdes. **5.x.4 + 5.x.3 — `RunState` granular a partir do journal + `BEGIN IMMEDIATE`**: novo módulo `state_mapping` (no `execution-engine`, não no `agent-engine` pra evitar ciclo) com `pub fn run_state_for_event(&StreamEvent) -> Option<RunState>` que mapeia cada evento (Delta → Streaming, ToolCall → WaitingToolCall, ToolResult → ValidatingToolResult, Done{Stop,Length} → Completed, Done{ToolCalls} → WaitingToolCall, Done{Error} → Failed, Error → Failed, Cancelled → Cancelled; Usage → None = sem mudança). 10 unit tests cobrem o mapping. A cada evento persistido pelo executor, se `run_state_for_event` devolve `Some`, chama `RunRepo::set_state_and_heartbeat_tx(run_id, state, last_event_seq)` numa **única transação** `BEGIN IMMEDIATE; ...; COMMIT;` que atualiza `state` + `last_heartbeat_at` + `last_event_seq` atomicamente (a Etapa 4 só chamava `set_status` que não é transacional). Novo método no `RunRepo`: `set_state`, `set_state_and_heartbeat_tx`, `list_stale_heartbeats`, `mark_interrupted`, `force_heartbeat_at_for_test`. **5.x.2 — Recovery de crash no startup**: novo módulo `recovery` (no `execution-engine`) com `recover_stale_runs` (lista runs não-terminais com `last_heartbeat_at` mais velho que `threshold` e marca como `interrupted` — a view `runs_with_status` mapeia `interrupted → timeout`) e `spawn_recover_stale_runs` (helper que recebe `Database` e constrói o `RunRepo` dentro do closure, sem `unsafe` — necessário porque a casca Tauri proíbe unsafe e o `RunRepo` tem borrow do `Database`). Threshold default `DEFAULT_STALE_THRESHOLD_SECS = 120` (maior que o `event_timeout` de 60s). Hook no `apps/desktop/src-tauri/src/main.rs::setup` chama `spawn_recover_stale_runs(db.clone(), Duration::from_secs(120))` em background (não bloqueia o startup). 2 tests em `tests/recovery.rs` (renomeado de `recovery` no `execution-engine/tests`; `recover_marks_only_stale_runs` + `recover_skips_terminal_runs`). **5.x.5 — Passo 10 de auditoria**: migration `0005_tool_audit.sql` cria tabela `tool_audit(id, run_id, tool_id, tool_version, arguments_json, result_ok, result_json, duration_micros, created_at)` (FK pra `runs` com `ON DELETE CASCADE`, índices `idx_tool_audit_run_created` e `idx_tool_audit_tool_created`). Append-only — sem `UPDATE` nem `DELETE` (mitiga R1 do `security-threat-model.md`). Novo `ToolAuditRepo` no `storage` (`append` + `list_for_run`). Novo trait `AuditSink` no `tool-registry` (`record(&self, entry) -> Result<(), String>`) com `NoopAuditSink` default e `RecordingAuditSink` pra testes. Novo `DbAuditSink` no `execution-engine::audit_sink` que implementa `AuditSink` via `ToolAuditRepo::append` (best effort — falha de I/O é logada via `tracing::warn!` mas não aborta a execução, conforme o spec do Passo 10). `RunExecutor::new` ganhou campo `audit_sink: Arc<dyn AuditSink>` (default `NoopAuditSink`); novo builder encadeável `RunExecutor::with_audit_sink(Arc<dyn AuditSink>)`. O `ChatOrchestrator::send_message` injeta `DbAuditSink::new(db.clone(), run_id)` ao montar o executor. O `RunExecutor::handle_tool_call` chama `audit_sink.record(AuditEntry { ... })` em todos os caminhos (aprovado+executado, rejeitado pelo validador) com `result_ok`, `arguments_json` raw, `result_json` serializado, e `duration` (medida com `Instant::now` antes/depois). 1 teste E2E em `tests/audit.rs` (`audit_records_files_read_execution`) valida que uma tool_call `files.read` aprovada+executada grava 1 entrada em `tool_audit` com `result_ok=true` e `arguments_json` correto. **Suíte do crate** `frederico-execution-engine`: **32/32 verde** (13 unit + 19 E2E = 10 state_mapping + 1 budget + 2 recovery + 6 e2e + 2 cancel + 4 recovery + 1 watchdog + 5 integration_orchestrator + 1 audit). **Suíte workspace 273/273 verde** em 35 targets (era 266 da Etapa 5.x; +3 accumulator + 1 anthropic tool_result) (era 252 da Etapa 4.x.y; +10 state_mapping + 2 recovery + 1 audit + 2 cancel_http no provider-engine + 1 cancel_http sanity). `cargo fmt --all -- --check` limpo; `cargo clippy --workspace --all-targets -- -D warnings` limpo; `check-core-purity.ps1` verde; `cargo tauri build` empacota NSIS `Frederico IA Studio_0.1.0_x64-setup.exe` em 2m 28s. `docs/modules/execution-engine.md` criado (RunExecutor, decisões não óbvias, API pública, cobertura de testes, diagrama de encaixe na arquitetura) e atualizado com as seções das Etapas 4.x (cancelamento + recovery), 4.x.y (reorganização dos crates), 5 (watchdog) e 5.x (5 sub-etapas). **Etapa 6.2 (frontend React)**: novo service `services/approvals.ts` (consome `AppOp::ApprovalList`/`Respond`); novo componente `components/ApprovalModal.tsx` (modal global com path, arguments em `<details>`, scope `Once`/`Run`/`Project`, reason opcional, badges Aprovar/Negar, badge `mandatory` quando for o caso). `App.tsx` polea `listApprovals()` a cada 2s via `setInterval` e renderiza o modal quando há entry pending. Estilo novo em `styles.css` (`.modal-backdrop`, `.modal`, `.approval-args`, `.btn-primary/danger`, `.badge/-warn`). `npm run build` verde — 49 modules, CSS 8.12 kB, JS 182.94 kB, build em 1.65s. **Fase 3 fechada em produção.** **Próximas etapas (da Fase 3):** **Etapa 6** (UI de aprovação com conteúdo real do `ApprovalRequest` + `PermissionSet` real do assistente/projeto + E2E em `tests/e2e/` com `files.read` de ponta a ponta — UI + Tauri + adapter real). | — |
| 4 | Memória e continuidade | concluída | **Etapas 1, 2, 3, 4, 5 e 6 fechadas** (fundação + retrieval híbrido com embeddings + classificador LLM pós-resposta + fluxo de correção/expiração/retomada + UI do painel de memória com `ScoreBreakdown` visível). 5 ADRs (0010-0014) + 2 specs aprofundados + novo crate `frederico-memory` (puro, `unsafe_code = "forbid"`) com 9 arquivos Rust. **Etapa 1**: `MemoryRepo` com 11 métodos (`insert_auto_captured` rejeita `ExternalContent`; `search_lexical` FTS5 com `OR` explícito + expansão de escopo; `mark_superseded` idempotente; `purge_expired` deleta soft-deleted + superseded + expirados-não-pinned). Migration `0007_memory.sql`: 5 tabelas (`memory_records` 24 colunas, `memory_fts` FTS5 + 3 triggers, `memory_embeddings` PK `(memory_id, provider, model)`, `embedding_reindex_jobs`). `LexicalRetriever` (BM25 + recência + importance + confirmation). `NoopEmbeddingAdapter` + `NoopMemoryClassifier`. **Etapa 2**: `OpenRouterEmbeddingAdapter` (HTTP OpenAI-compat, default OpenRouter + `text-embedding-3-small`, 1536 dim, auth via `SecretString`/DPAPI, timeout 1.5s); `embedding_codec.rs` (encode/decode f32 little-endian + `cosine_similarity`); `EmbeddingWorker` (tokio task em background, batch 50, idempotente, sem retry em loop); `HybridRetriever` (5 sinais do `ADR-0011 §4`: lexical + recency + semantic + importance + confirmation, timeout total 2s, fallback lexical se provider indisponível, `cosine` clipado em ≥ 0); `RETRIEVER_BUDGET = 2s` const. **Gold-set** (10 cenários) + **Runner** (`tests/evaluation.rs`) roda **2 testes E2E**: `run_gold_set_evaluation` (baseline lexical, 10/10 F1=1.0) + `run_gold_set_evaluation_hybrid` (híbrido com `FakeHashEmbed`, 10/10 F1=1.0, gate `F1_híbrido ≥ 0.9` → não regride o baseline). Vazamento cruzado 0 (I4 atendido). p99 ~1ms. **`Database::open_in_memory()`** no storage (max_connections=1, pro runner ser determinístico). **`scoring.toml` + `eval.toml`** versionados. **`sanitize_fts5_query`** neutraliza caracteres perigosos + junta tokens com `OR` (FTS5 padrão usa AND implícito, matava BM25 parcial). **Decisão de empacotamento:** `Clock` continua no `frederico-security` (mover pro `core` é trabalho futuro, ADR-0014). **Etapa 3**: `CompletionProvider` trait + `NoopCompletionProvider` (porta fina pro `provider-engine` sem dependência cíclica) + `LlmMemoryClassifier` (LLM-based, default `openai/gpt-4o-mini` via OpenRouter, prompt restrito, parse de JSON, validação de scope/confidence/importance, threshold 0.6 default, cota 5/min via `Mutex<Vec<Instant>>` — regra do `ADR-0012 §2`). `MemoryExtractionJob` (run_id, conversation_id, messages, finished_at) + `MemoryExtractor` + `MemoryExtractorHandle` (worker pós-resposta, `tokio::spawn` em background, canal mpsc 256, sem `tokio::time::interval` — ADR-0014 §1). `determine_real_origin` helper (regra do `ADR-0012 §3`): sobrescreve o `origin` proposto pelo LLM com base na proveniência real das mensagens — se qualquer tem `source` começando com `tool_output:` ou `document_attachment:`, vira `ExternalContent` e vai pra `insert_pending_review` (UI da Etapa 5 consome a fila); se há mensagem `assistant` ou `tool`, vira `Assistant` (consolidação do modelo); senão, segue o que o LLM propôs (default `User`). Mitiga **E2** do security-threat-model.md em camada dupla (classificador sugere + worker decide). 7 testes novos do `MemoryExtractor` (User→auto_captured, external→pending_review, assistant→Assistant, classifier=None não insere, classifier=Err não panica, NoopMemoryClassifier compat, enqueue não-bloqueante). Renomeia `WorkerCommand`→`EmbeddingCommand` e `WorkerTask`→`EmbeddingTask` (coexiste com `ExtractorCommand`/`ExtractorTask`). `lib.rs` re-exports `MemoryExtractionJob, MemoryExtractor, MemoryExtractorHandle`. 6 testes do `LlmMemoryClassifier` no `classifier.rs` (NoopCompletion, parse válido, threshold baixo, JSON inválido, quota estourada). **Etapa 4**: `MemoryRepo::apply_correction(old_id, replacement, now) -> CorrectionResult` — fluxo atômico de "corrija para X" (`ADR-0014 §2`): valida replacement → BEGIN IMMEDIATE → insere a nova (com `user_confirmed = true`, `pending_review = false`) → marca a antiga como superseded (idempotente) → COMMIT. Devolve `CorrectionResult { old_id, new_record, superseded_at }` pro caller (UI da Etapa 5 mostra "essa memória foi substituída por X há 3 dias"). `MemoryRepo::list_pending_review(now)` — fila de revisão de ExternalContent (consumida pelo painel). Wire do `purge_expired` na inicialização (casca Tauri, `main.rs::setup`, best-effort). Regra "`user_pinned` bypassa `expires_at` mas não `superseded_by`" enforçada nas queries do `list_by_scope`, `list_pending_review` e `purge_expired`. `frederico-memory` + `chrono` adicionados como deps do `frederico-desktop`. 9 testes E2E novos em `crates/memory/tests/expiration.rs`. **Etapa 5**: IPC backend (`shared-contracts` + `main.rs`) ganha 6 `AppOp` (`MemoryList`, `MemoryRetrieve`, `MemoryApplyCorrection`, `MemoryConfirmPending`, `MemoryRejectPending`, `MemoryPurgeExpired`) + 4 views (`MemoryView`, `ScoreBreakdownView`, `MemoryHitView`, `CorrectionResultView`) + 1 input (`NewMemoryInputView`). `MemoryRepo::list_pending_review` consumido pelo IPC. 9 testes E2E roundtrip + 3 testes de view no `shared-contracts` (17/17 verde). `MemoryExtractor` wire no `ChatOrchestrator` (Etapa 3 → Etapa 5 fechada): campo `memory_extractor: Option<Arc<MemoryExtractorHandle>>` + helper `build_extraction_job` carrega as últimas 6 mensagens da conversa e enfileira um `MemoryExtractionJob` após cada Run finalizar (Completed ou Failed). Casca Tauri cria o `LlmMemoryClassifier` (com `NoopCompletionProvider` por enquanto — a Etapa 5.x injeta o `OpenRouterCompletionProvider` real) + `MemoryExtractor` em background, e passa o handle pro `ChatOrchestrator::new` (11º argumento). Frontend React: novo `services/memory.ts` (camada IPC, regra do `ADR-0003`), novo `components/MemoryPanel.tsx` (lista com `ScoreBreakdown` visível — `PROMPT MESTRE` §10.11 — + badges pinned/pendente/expirada/superseded + form inline de "corrija para X" + banner "Memória substituída" com link), nova rota `/memories` em `routes/Memories.tsx`, link "Memórias" no nav do `App.tsx`. `tsc --noEmit` + `npm run build` verdes — 52 modules (era 49; +3), CSS 10.22 kB (era 8.12 kB; +2.1 kB), JS 191.62 kB (era 182.94 kB; +8.7 kB), build em 624ms. **Etapa 6** (gate de CI): `tests/fixtures/gold_set.jsonl` expandido de 10 → 21 cenários cobrindo as 17 categorias do plano de avaliação (escopo project/client, correção de info antiga, expiração com pinned bypass, vazamento cruzado entre projetos, sinônimos, cross-language, prompt injection como dado, malicious memory, documento anexo com payload → `pending_review`, duplicate conflict, no_results, greeting curto, message_ok). `config/eval.toml` com alvos de produção (hard fail em CI): precisão ≥ 0.70, F1 ≥ 0.65, vazamento cruzado = 0 (I4), p99 ≤ 2000ms (§10.13), p95 ≤ 1000ms (warning). `tests/evaluation.rs` runner agora carrega o `eval.toml` em tempo de teste (`load_eval_gate` parse manual — evita dep nova de `toml`); corrige bug do `run_scenario` que usava `insert_auto_captured` direto (agora escolhe baseado em `user_confirmed` + `origin`). `Default for EvalGate` agora reflete os alvos da Etapa 6 (era Etapa 1 permissivo). Medido: P 0.905, F1 0.937, vaza 0, p99 1-3ms (gate 2000ms — folga de 3 ordens de grandeza). 21/21 verde nos 2 testes do runner. Suíte workspace: 374/374 verde (era 365; +9 do shared-contracts). `cargo fmt`, `cargo clippy --workspace --all-targets -- -D warnings`, `check-core-purity.ps1` todos limpos **Consolidada no `main` pelo commit de merge `91fdd53`** (PR #1, `fase-4/etapa-1-fundacao`, 16 commits). Specs `memory-architecture.md` e `memory-evaluation-plan.md` carimbados `parcialmente implementado` em 2026-07-28; `CHANGELOG.md` atualizado. | **Pendência de empacotamento:** mover `Clock` do `frederico-security` para o `frederico-core` (4 call sites da Fase 2/3). **Pendência de gold-set:** os 10 cenários originais (Etapa 1) tinham BM25 perfeito (1.0 no lexical) — Etapa 6 expandiu pra 21 cobrindo sinônimos/paráfrases (cobre parte do ganho do semântico, mas o completo exige `OpenRouterEmbeddingAdapter` real). **Pendência da Etapa 5.x:** o `NoopCompletionProvider` é o default atual — quando a Etapa 5.x injetar o `OpenRouterCompletionProvider` real, o classificador vai usar LLM de verdade (custo de API por classificação, mas cota 5/min já enforced). |
| 5 | Documentos | em andamento | **Etapa 1 fechada**: crate `frederico-document-engine` (puro, `unsafe_code = "forbid"`) com `DocumentSpec` (20 blocos: Cover, Toc, Heading, Paragraph, List, Table, KeyValue, Kpis, Callout, Quote, Steps, Chart, Image, Code, Divider, Spacer, PageBreak, Footer, Signatures, BackCover) + validação em 2 camadas (JSON Schema gerado em runtime via `schemars`, cache thread-safe em `OnceLock`; regras semânticas em Rust: `Kpis` 2-4, `Spreadsheet` apenas Kpis/Table/Chart, `language` minúsculas, `blocks` não vazio, `spec_version` MAJOR.MINOR.PATCH, `Table` linhas com mesmo nº de colunas) + função pura `document_mode_prompt()` que gera o prompt do modo documental a partir do catálogo de blocos (REGRAS §1.9). Suíte do crate: 22/22 verde (2 unit + 20 integration). `cargo clippy --workspace --all-targets -- -D warnings` limpo. 4 specs (`document-engine-architecture.md`, `wordpro-specification.md`, `excelpro-specification.md`, `pdfpro-specification.md`) promovidos pra `parcialmente implementado`. **Etapa 2A fechada (commit próprio, ver ADR-0015 + ADR-0016)**: crate `frederico-process-architecture` ganha `WorkerManager` + `WorkerHandle` redesenhados como **ator** (sem `Arc<Mutex<Box<dyn Pipe>>>`). Trait `Pipe` única foi **dividida** em `PipeReader` (não `Clone`, leitura serializada pela task do ator) + `PipeWriter` (`Clone` + `Send` + `Sync`, escrita concorrente via `mpsc::Sender` ou `Arc<NamedPipeServer>`). `invoke`/`ping`/`shutdown` mandam comandos por `mpsc::Sender<ManagerCommand>` interno + `oneshot::Sender` correlacionado por `request_id` que o **ator** gera (não o caller). Handshake síncrono no `WorkerManager::spawn_in_process` (lê `worker.hello` do fake, gera `WorkerAuth`, envia `app.ack`, spawna o ator) — ator não vê o `hello` inicial. `pending: Mutex<HashMap<...>>` é o único lock, usado em `insert`/`remove` síncronos (sem `.await` dentro do lock). Suporta **invocações concorrentes** sem lock no caminho do `invoke` (cada invoke tem seu próprio `oneshot`). Novo crate `frederico-test-support` com `with_test_timeout` (5s default) — embrulha todo teste de integração de worker pra que deadlock vire falha com nome do teste em 5s, não sessão pendurada. **Trava do CI** (medida 4 do ADR-0015): `-D clippy::await_holding_lock` agora no `ci.yml` (GitHub Actions) além do `verify.ps1` local. **`FakeWorker`** envia `worker.hello` no boot (bugfix — antes era no-op), suporta `slow_response_ms` (atraso artificial pra testes de timeout). Suíte do crate: **9/9 unit + 10/10 integration verde** em 0.5s (incluindo `concurrent_invocations_complete` que prova o caso que quebrava a Etapa 2A original, e `invoke_with_short_timeout_fails` que prova o timeout). Suíte workspace **49 grupos, 0 falhas**. `cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D warnings -D clippy::await_holding_lock`, `check-core-purity.ps1` todos limpos. `docs/modules/process-architecture.md` atualizado pra refletir o manager redesenhado. **Pendência registrada:** `docs/architecture/process-architecture.md` (spec) ainda está `especificado`; precisa ser promovido pra `parcialmente implementado` na Etapa 3 (quando o `docs.generate` consumir o `WorkerHandle::invoke`). **Próxima etapa (da Fase 5):** Etapa 2B — `WindowsPipeReader`/`Writer` via `windows` crate (gateado) + `spawn_external` (`tokio::process::Command`) + bootstrap do `document-worker.exe` Python (Tesseract, fontes "Tinta & Latão"). **Run de CI verde (Regra §2.2)**: workflow `CI` job `verify (Windows)` com `conclusion: SUCCESS` no SHA `3dededc` (run [#30466635528](https://github.com/fredabsd-svg/Frederico-IA-Studio-review/actions/runs/30466635528), concluído em 2026-07-29T15:41:08Z) — inclui a trava de CI `-D clippy::await_holding_lock` no `ci.yml` (medida 4 do ADR-0015). **Etapa 2B fechada (PRs #9 + #10 + #11 — 2026-07-29)**: transporte real sobre named pipes do Windows via `tokio::net::windows::named_pipe` (Etapa 2B, ADR-0017, 3 unit + 2 integration verde em <100ms — os 2 integration saíram do `#[ignore]` depois do fix do deadlock do `ConnectNamedPipe` na Etapa 2B continuação, `connect()` no server em vez de `ready()` pré-connect). **`WorkerManager::spawn_external` (Etapa 2B continuação)** — `tokio::process::Command` com `stdin = null`, `stdout`/`stderr` piped, `CREATE_NO_WINDOW`; lê `READY <pipe_name>` do stdout com timeout 10s; `connect_pipe_client` em `spawn_blocking` + `client.ready(READABLE | WRITABLE).await`; segue handshake `worker.hello`/`app.ack` (mesmo do `spawn_in_process`); devolve `(WorkerManager, WorkerHandle)` indistinguível do fake. `WorkerManager` ganhou campo `child: Option<Child>`; o `shutdown` faz `child.wait()` com timeout 5s + `kill` (TerminateProcess) se o worker não respondeu ao `app.shutdown`. `ExternalSpawnConfig` re-exportado no `lib.rs` (`#[cfg(windows)]`). **PATH do pai é injetado automaticamente** (exceção documentada do §Invariantes — workers precisam pra resolver DLLs/binários; PATH não é segredo). **`IpcMessage::decode_line` tolera BOM UTF-8** e **`\r\n`** (defesa em profundidade — `StreamWriter` do .NET e PowerShell enviam assim por default). **`IpcOp` serializa com o nome do contrato** (`worker.hello`, `app.ack`, etc) — bug sutil da Etapa 2A descoberto quando o stub PowerShell enviou o nome correto: `Hello` serializava como `"hello"` (sem prefixo). Custom `Serialize`/`Deserialize` substitui `rename_all = "snake_case"`. 3 integration tests E2E novos em `tests/external_worker.rs` (`external_spawn_roundtrip_full_protocol` com stub PowerShell que implementa protocolo completo; `external_spawn_fails_when_command_missing`; `external_spawn_timeout_when_no_ready` validando que `child` é morto antes de retornar erro). 5 unit tests novos em `src/external.rs` (`parse_ready_line_*`). **`document-worker` Python stub (Etapa 2B continuação)** em `workers/document-worker/` — `document-worker.py` (~250 linhas) com `manifest.json` (worker_id=document-worker, 7 capabilities: docx/xlsx/pdf write+read + ocr.run), protocolo completo (worker.hello/app.ack/app.ping/app.shutdown/tool.invoke/tool.result/worker.error), `pywin32` pra `CreateNamedPipe`/`ConnectNamedPipe`/`ReadFile`/`WriteFile`, handlers **stub** que retornam `tool.result { ok: false, code: "handler_stub" }` (handlers reais entram em etapas seguintes). **`bootstrap.ps1`**: baixa Python 3.12.7 embeddable (~10 MB) do python.org, extrai em `runtime/`, baixa `get-pip.py`, edita `python312._pth` pra liberar site-packages, instala `pywin32`. Idempotente (pula se `runtime/python.exe` já existe). README + `.gitignore` (ignora `runtime/`, `__pycache__/`) no dir. Suíte do crate `frederico-process-architecture`: **32/32 verde** (12 unit + 10 fake_worker integration + 2 windows_pipes_smoke integration + 5 external unit + 3 external_worker integration). Suíte workspace: **51 grupos, 430 passed, 0 failed, 3 ignored** (+8 vs Etapa 2B parcial). `cargo fmt --all -- --check` limpo; `cargo clippy --workspace --all-targets -- -D warnings -D clippy::await_holding_lock` limpo; `check-core-purity.ps1` OK. **Run de CI verde (Regra §2.2)** em 2026-07-29 no SHA exato do PR #11 (registrado na próxima entrada de PR — Etapa 2B fecha quando este PR mergeado + verde em CI). Documentação atualizada: `docs/modules/process-architecture.md` (novas peças, §6 "o que não faz" reescrita, §Pendências reduzida pra 4 itens); `docs/architecture/process-architecture.md` (carimbo 2026-07-29, §"Descoberta" com passo 2 detalhado do `spawn_external`); `CHANGELOG.md` (entrada Etapa 2B continuação no topo); `workers/README.md` (atualizado de "vazio" pra listar o `document-worker`); `workers/document-worker/README.md` (novo, layout + status + como rodar). **Próxima etapa (da Fase 5):** Etapa 2B+X — Tesseract + fontes "Tinta & Latão" no `bootstrap.ps1` estendido + handlers reais do `document-worker` (`docx.write`/`ocr.run`/...) consumindo o `document-engine` (Etapa 1, já fechado). Em paralelo, Etapa 3 (integração com o `ToolRegistry`) — `docs.generate` consome `WorkerHandle::invoke`. **Run de CI verde (Regra §2.2) do PR #11 (Etapa 2B fechando)**: workflow `CI` job `verify (Windows)` com `conclusion: SUCCESS` no SHA exato `b1f6f1e` ([run #30543829705](https://github.com/fredabsd-svg/Frederico-IA-Studio-review/actions/runs/30543829705), 4m 32s, 2026-07-30T09:45 GMT-3). PR mergeado em `main` = `945963f` (squash). **Bumps na SHA** (mesmo PR, 5 commits intermediários) — REGRAS §2.4 (Vermelho não se contorna): (1) `kill_on_drop(true)` no `Command` resolveu o travamento de 6h do `cargo test` no CI (processo PowerShell zumbizando); (2) `with_test_timeout` do test E2E pra 15s (default 5s não coube); (3) `ready_timeout` do test E2E pra 30s (default 10s não coube); (4) `with_test_timeout` do test E2E pra 60s (handshake E2E completo levou > 15s no CI); (5) **substituição do stub PowerShell por binário Rust** (`crates/process-architecture/src/bin/worker_stub.rs`, `[[bin]]` no Cargo.toml) — cold-start ~50ms vs PowerShell 5.1 ~500ms+ local / > 30s no Windows Server 2022 runner do GitHub Actions. Stub PowerShell mantido em `tests/stubs/worker-stub.ps1` como referência. **Lição de processo (cross-project, vai pra agent memory):** quando o test E2E precisa spawnar um processo, **prefira binário Rust via `[[bin]]`** em vez de runtime externo (Python/PowerShell/Node) — elimina variabilidade de cold-start do runtime no CI runner. **`kill_on_drop(true)` é obrigatório** quando o processo pode ser dropado antes do `shutdown` explícito (cenário comum: `with_test_timeout` cancela o test antes do `manager.shutdown()`). **Run de CI verde (Regra §2.2)** anterior: Etapa 2A SHA `3dededc` ([run #30466635528](https://github.com/fredabsd-svg/Frederico-IA-Studio-review/actions/runs/30466635528)). Pendência anterior (`docs/architecture/process-architecture.md` ainda `especificado`) **resolvida** — o spec foi promovido pra `parcialmente implementado` no commit que fechou a Etapa 2B (header carimbado 2026-07-29, `Fase correspondente: 5`). **Etapa 2B+X fechada (2026-07-30):** `document-worker` Python v0.2.0 com **6 handlers reais** (`docx.write`/`docx.read` via `python-docx`, `xlsx.write`/`xlsx.read` via `openpyxl`, `pdf.write`/`pdf.read` via `reportlab` + `pdfplumber`) consumindo as bibliotecas da ADR-0004. Fontes "Tinta e Latao" (Adobe Source Sans 3 + Source Serif 4 TTF variable) embutidas no PDF (TTF e nao OTF - bug Windows 10/11 com CFF2 variable, ADR-0018 §Decisao 2b). `bootstrap.ps1` estendido idempotente: Python 3.12.7 + pywin32 + python-docx + openpyxl + reportlab + pdfplumber + 4 TTFs (~120 MB, cacheavel por `actions/cache@v4`). Path safety minima no handler Python (rejeita `..`); allowlist forte fica pra Etapa 3 (`ToolManifest::allowed_paths`) + sandbox pra Fase 7 (`sandbox-runner`). **Limitacao conhecida do `pdf.read`:** PDFs 100% escaneados devolvem `code: pdf_scanned_no_ocr` no payload (`scanned_pages: [n, m, ...]` lista paginas sem texto em PDFs mistos) - OCR entra na Etapa 2B+Y. **ADR-0018 (novo) trava o papel do handler como primitiva de baixo nível** - o kit (Etapa 3, `WordPro`/`ExcelPro`/`PdfPro`) renderiza o `DocumentSpec` chamando os 6 handlers, sem reescreve-los. **`ocr.run` REMOVIDO do `manifest.json`** (vai pra 2B+Y sozinho - 1 capability de 7 puxava 90% do peso, do risco e da variabilidade "funciona na minha maquina"). **6 testes E2E em `tests/external_doc_worker.rs`** (NAO `#[ignore]` - rodam em CI em todo PR) cobrem todos os handlers + path safety + capability desconhecida + font_status carregado. **`scripts/verify-external.ps1`** (novo) faz o bootstrap idempotente + os 6 E2E; cache `document-worker-runtime` no `.github/workflows/ci.yml` (key: hash de `pyproject.toml` + `bootstrap.ps1`). Suíte workspace **44 grupos de teste, 430 passados, 0 falhas, 3 ignored** (era 38 do PR #11 - +6 E2E novos). **Etapa 2B+Y fechada (2026-07-30):** `document-worker` Python v0.3.0 com 7º handler `ocr.run` (consumindo `pytesseract` 0.3.10+ + Tesseract 5.4.0.20240606 do UB-Mannheim GitHub Releases, silent install NSIS com admin detection) + `por`+`eng`+`osd` traineddata (`tessdata_fast` 4.1.0, SHA-256 fixo por arquivo + tag do release, **não `raw/main`**) + `pdf.read` com fallback OCR transparente (`text` e `ocr_text` **sempre separados** — procedência, mesma disciplina de `origin`/`external_content` da memória; OCR troca 8 por B, 0 por O, 1 por l, e é exatamente em CNPJ/competência/valor que o erro cai), parâmetro `ocr: "auto"|"never"|"only"`, teto de 20 páginas (`MAX_OCR_PAGES_PDF`) com `ocr_truncated: true`, `tesseract_version` no retorno (reprodutibilidade), `PDF_FALLBACK_OCR_LANG = "por"` (contexto brasileiro), validação rigorosa de `lang` (regex `^[a-z]{3}(+[a-z]{3})*$` + checagem contra `INSTALLED_OCR_LANGS` — defesa contra injeção de argumento). **Mudança visível do `pdf.read`:** PDF 100% escaneado que antes retornava `ok: false, code: pdf_scanned_no_ocr` agora pode retornar `ok: true` com `text` do OCR + `extraction: "ocr"` (CHANGELOG registra como breaking change). **Bootstrap estendido** com admin detection: silent install do NSIS se admin, pula com warning + instruções se não-admin (PowerShell 5.1 bloqueia `Start-Process` antes de executar .exe com manifesto `requireAdministrator`); SHA-256 do instalador e de cada traineddata validados antes de usar; verificação de portabilidade obrigatória na 1ª execução (copia `runtime/tesseract` pra outro path, seta `TESSDATA_PREFIX`, roda `--list-langs` — se a árvore depender de registro/var global, ADR-0019 §Plano alternativo entra: extração NSIS via dep externa, registrada). **5 testes E2E novos em `tests/external_doc_worker.rs`** (11 testes totais agora): `e2e_ocr_run_with_real_image` e `e2e_pdf_read_with_ocr_fallback_on_scanned` (com Tesseract, panics claros se ausente) + `e2e_ocr_run_with_invalid_lang` (code `invalid_lang`), `e2e_ocr_run_without_tesseract` (code `ocr_not_available`), `e2e_pdf_read_with_ocr_param_never` (procedência testada — `ocr_text: {}` separado de `text`). Comparação OCR normalizada (lowercase + colapso de espaços) — Tesseract troca 1 por l, 0 por O, 5 por S; comparação exata quebra em 100% dos casos reais. **`e2e_unknown_capability_rejected` ajustado** pra usar `pdf.print` (não `ocr.run` que agora é conhecido). **`docs/modules/document-worker.md` (novo, REGRAS §1.4)**: 6 seções (o que faz / o que expõe / de que depende / decisões não óbvias / como testar / **o que NÃO faz** + limites explícitos); lista os 7 handlers com bibliotecas, dependências com SHA-256 fixo, armadilhas conhecidas (pytesseract opcional, bootstrap best-effort non-admin, normalização OCR no E2E, teto de páginas), pendência (Etapa 3 - kits DocumentSpec com `ToolManifest::allowed_paths` forte). **CI noturno isolado** (`.github/workflows/ci-nightly.yml`, cron `0 4 * * *` UTC = 01:00 BRT madrugada Brasil = 21:00 PDT noite EUA, pouco uso do GitHub Actions runners): roda mesma suíte (cargo test + verify-external com Tesseract instalado) em contexto separado, **falha NÃO bloqueia merge** — gate principal (`ci.yml`) continua intocado. `windows-latest` runner roda como admin → silent install NSIS funciona. **`workflow_dispatch` manual** pra rodar on-demand (bump de dep, investigar flakiness). **ADR-0019 (novo)**: 5 decisões (Tesseract source com admin detection + verificação de portabilidade + plano alternativo; `tessdata_fast` 4.1.0 com `por+eng` default + `lang` parametrizável com validação rigorosa; `text`/`ocr_text` separados com procedência + `ocr` param auto/never/only + teto de 20 páginas + `tesseract_version` no retorno + `ocr_truncated`; CI noturno isolado; comparação OCR normalizada no E2E com justificativa). **Suíte do crate `frederico-process-architecture`**: 38/38 verde (era 32 da 2B+X; +5 OCR + 1 unknown_capability ajustado). **Suíte workspace**: 44 grupos, **430 passed, 0 failed, 3 ignored** (sem mudança de contagem, só suite ampliada). **`cargo fmt --check`**, **`cargo clippy --workspace --all-targets -- -D warnings -D clippy::await_holding_lock`**, **`cargo test --workspace`**, **`scripts/check-core-purity.ps1`**, **`node scripts/check-docs.mjs`**, **`node scripts/check-doc-impact.mjs`** — todos limpos. **Pendência registrada (próxima etapa):** Etapa 3 — `ToolRegistry` consome `WorkerHandle::invoke`, kits `WordPro`/`ExcelPro`/`PdfPro` renderizam `DocumentSpec` chamando os 7 handlers da v0.3.0 (handler = primitiva, kit = renderer, ADR-0018 §Decisão 1 — **sem reescrita**). `ToolManifest::allowed_paths` fecha a path safety forte. `docs.generate` é o caso de uso principal. **Run de CI verde (Regra §2.2)** do PR [#12](https://github.com/fredabsd-svg/Frederico-IA-Studio-review/pull/12) (Etapa 2B+Y fechando): workflow `CI` job `verify (Windows)` com `conclusion: SUCCESS` no SHA exato `1192d7b` ([run #30629061027](https://github.com/fredabsd-svg/Frederico-IA-Studio-review/actions/runs/30629061027), job [#91150961780](https://github.com/fredabsd-svg/Frederico-IA-Studio-review/actions/runs/30629061027/job/91150961780), 4m 55s, 2026-07-31T09:00 GMT-3). **PR mergeado em `main` = `d7b13c8` (squash) em 2026-07-31.** **Bumps na SHA** (mesmo PR, **5 commits intermediários** alem do `fbde36a` original — saga de CI com **6 runs ate o verde**): (1) `2d447e7` (ordem do step E2E no `ci.yml` — `cargo test --workspace` re-roda os integration tests, e sem bootstrap antes o `python.exe` nao existia); (2) `e924826` (tentativa falha de remover pre-create do silent install — palpite errado, NAO era a causa real); (3) `bb94d90` (silent install no path default do Windows + `Copy-Item -Recurse` pro `runtime/tesseract/` — `/D=` e ignorado pelo instalador UB-Mannheim por causa do NSIS macro `MULTIUSER_INSTALLMODE_INSTDIR`, issue `tesseract-ocr/tesseract#4360`); (4) `2bbfa71` (3 testes E2E `e2e_pdf_write_and_read`/`e2e_pdf_read_reports_ocr_unavailable`/`e2e_ocr_run_without_tesseract` ficam adaptativos via helper `is_tesseract_available()` — antes hard-coded `false`/`ocr_not_available`, agora branch pelo estado do runtime); (5) `1192d7b` (timeout `wait_for_status` do `frederico-provider-engine/tests/recovery.rs` 2s→5s — flake do CI Windows Server 2022, 3-5x mais lento que dev local). **Lições de processo (cross-project, vai pra agent memory):** (a) **CI step ordering quando bootstrap instala runtime:** se o `Tests` chama `cargo test --workspace` e o bootstrap e em outro step, o bootstrap PRECISA rodar antes — o `cargo test --workspace` em si nao diferencia unit de integration tests. (b) **NSIS `MULTIUSER_INSTALLMODE_INSTDIR` silently overrides `/D=<path>`** no instalador UB-Mannheim (e provavelmente outros instaladores NSIS com essa macro). Workaround generico: silent install no path default + `Copy-Item -Recurse` pro nosso target. (c) **Testes adaptativos ao ambiente > testes hard-coded:** se um teste depende de um binario externo, faca-o branchear entre "tem" e "nao tem" via filesystem check. NAO use `#[ignore]` (teste pulado e regressao silenciosa). (d) **Timeouts de CI Windows Server 2022:** multiplique por 3-5x o timeout que passa em dev local (2s flakea em CI; 5s folgado sem mascarar regressao). (e) **Diagnosticar via web search e issue tracker ANTES de chutar:** o `/D=` ignorado ja tinha issue aberta (`tesseract-ocr/tesseract#4360`) — 5 min de busca economizou 2 commits de tentativa-e-erro (`e924826` foi palpite errado de pre-create). Branch `fase-5/etapa-2b-y-tesseract-ocr` deletada (local + remoto). |
| 6 | Multimodelo e subagentes | não iniciada | — | depende da Fase 3 + 4 |
| 7 | Modo desenvolvedor | não iniciada | — | depende da Fase 3 |
| 8 | Copiloto, tarefas e refinamento | não iniciada | — | depende de 3 + 4 + 6 + 7 |
| 9 | Produção | não iniciada | — | depende de todas |
