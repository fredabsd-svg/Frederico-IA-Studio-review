# Estado Real por Fase

Primeiro arquivo a ser lido por qualquer sessão nova de IA, **depois de** `REGRAS-DO-PROJETO.md`.

## Estados possíveis

- `não iniciada` — fase planejada, nenhum trabalho começou.
- `em andamento` — código está sendo escrito; testes da fase ainda não todos verdes.
- `concluída` — todos os testes da fase passam; specs correspondentes promovidos a `parcialmente implementado` ou `implementado`; changelog atualizado.
- `bloqueada` — algo impede progredir; motivo documentado na coluna "Pendências".

## Regra de promoção

Promover uma fase de `em andamento` para `concluída` exige, simultaneamente:

1. Suíte de testes da fase 100% verde.
2. Specs correspondentes com `Estado` atualizado para `parcialmente implementado` ou `implementado`, com carimbo de verificação recente.
3. Entrada em `CHANGELOG.md` descrevendo o efeito para o usuário.
4. Referência ao PR / commit que consolidou a fase.
5. Colunas `E2E de cobertura` e `Passo CI` da tabela preenchidas e consistentes com o código (gate `check-e2e-gate.ps1` da REGRA §3 verde).

## Tabela

| Fase | Nome | Estado | Evidência | Pendências | E2E de cobertura | Passo CI |
|------|------|--------|-----------|------------|------------------|----------|
| 0 | Fundação documental | concluída | este `status.md`; PR de fundação documental; 9 specs em `docs/architecture/`; 4 ADRs em `docs/decisions/`; `REGRAS-DO-PROJETO.md` com §1.13 | regra não-aplicável: Fase documental sem código de produção| - | - |
| 1 | Fundação (Tauri + Rust + SQLite) | concluída | suíte workspace 15/15 verde (`cargo test`); `cargo clippy --workspace -- -D warnings` limpo; `npm run build` verde; `scripts/check-core-purity.ps1` OK; `cargo tauri build` produz `target/release/bundle/nsis/Frederico IA Studio_0.1.0_x64-setup.exe` (3,0 MB) | lacunas: Tauri build §28.5 (instalador não exercitado em CI), migrations on restart, reload de janela §32 (sem §28.5 máquina limpa ainda); cobertos parcialmente pelo E2E de path do produto abaixo| crates/e2e/tests/e2e_files_read.rs::files_read_e2e_through_chat_orchestrator | cargo test --workspace |
| 2 | Chat e provedores | concluída | Etapas 1, 2, Leva 3 e Etapa 5 (UI) fechadas; **Hardening 1 (DPAPI real) fechado**: `WindowsCredentialStore` implementado via `CredWriteW`/`CredReadW`/`CredDeleteW`/`CredEnumerateW` (crate `windows` v0.58), `TargetName` `Frederico-IA-Studio:provider:<id>`, mapeamento correto de HRESULT→win32 (`& 0xFFFF`), `CredFree` chamado uma única vez por alocação (evitando double-free); 6 testes de integração (`tests/windows_credential_store.rs`) com mutex global de serialização e IDs únicos por run: set/get roundtrip, get-nonexistent→None, delete idempotente, list filtra prefixo Frederico, list filtra prefixo + valida que credencial de outro app não vaza, overwrite de credencial existente. Casca Tauri: `AppState.credentials: Arc<WindowsCredentialStore>` único no processo; `ProviderSetCredential`/`ProviderDeleteCredential` agora chamam DPAPI real (não mais `FakeCredentialStore` ad-hoc). `Cargo.toml` de `frederico-security`: `unsafe_code = "deny"` (era `forbid`); `#![allow(unsafe_code)]` apenas no módulo `windows.rs` e nos tests. `scripts/check-core-purity.ps1` reconhece `crates/security/src/windows*` e `crates/security/tests*` como exceções legítimas. **Hardening 3 (`provider-recorder`) fechado**: novo binário `recorder` em `crates/provider-engine/src/bin/recorder.rs` que faz chamada real a um provedor OpenAI-compat (OpenAI/OpenRouter/DeepSeek/Mistral/NVIDIA NIM/Ollama/LM Studio), captura os bytes brutos do stream SSE e grava em `fixtures/<provider>/<scenario>.jsonl` com header `# recorded_at=... provider=... model=... base_url=... format=openai-compat-sse`. **Sanitização obrigatória**: módulo `frederico_provider_engine::sanitize` (regex case-insensitive bloqueando `Authorization`/`api_key`/`x-api-key`/`Bearer `/`sk-`/`sk-ant-`/`gsk_`/`or-`); o recorder chama `sanitize::check` antes do flush e, se falhar, **deleta o arquivo** e aborta com exit 1. Integration test `tests/fixtures_sanitize.rs` varre `fixtures/**/*.jsonl` no CI e quebra o build se algum estiver contaminado. Leitura da chave via `--api-key-env VAR` (default `OPENAI_API_KEY`) — nunca via `CredentialStore` (a chave do recorder é descartável, do CI/dev, não do app). **Hardening 4 (contract tests off-PR) fechado**: 3 integration tests em `crates/provider-engine/tests/openai_compat_contract.rs` (OpenAI + OpenRouter) e `crates/provider-engine/tests/anthropic_contract.rs` (Anthropic). Cada um gateado em env var (`OPENAI_API_KEY`/`OPENROUTER_API_KEY`/`ANTHROPIC_API_KEY`); se ausente, o test **pula** com mensagem clara em vez de falhar — assim a suíte roda limpa no PR (sem chave) e roda de verdade no CI noturno (com chave). Os tests validam que o adapter produz pelo menos 1 `Delta` e exatamente 1 `Done` com `StopReason` válido, contra a API real. Helper compartilhado em `tests/common/mod.rs` (`require_env`, `openai_adapter_or_skip`, `drain_events`). Runner dedicado: `scripts/run-contract-tests.ps1` (mostra quais env vars estão setadas, mascarando a chave; roda `cargo test --test openai_compat_contract --test anthropic_contract`). **Hardening 5 (recovery E2E) fechado**: 3 integration tests em `crates/provider-engine/tests/recovery.rs` que validam a regra do **"Journal de eventos"** — o SQLite é a fonte de verdade, sobrevive a drop do orquestrador. `journal_persists_events_across_orchestrator_drop` dispara run com 5 deltas + Done, espera completar, dropar orquestrador A, reabre db com orquestrador B, valida que `MessageEventRepo::list_for_message` retorna os mesmos 5 deltas + Done. `cancel_idempotent_and_status_persists` valida que `cancel_run` é idempotente e o status final (Completed/Cancelled/etc.) bate entre sink e db após recovery. `user_message_persisted_before_run_starts` valida que a `Message.role=user` está no db imediatamente após `send_message` retornar (regra do "Journal-then-emit" aplicada também à user msg). Suíte workspace **133/133 verde** (era 130/130; +3 do recovery); `cargo clippy --workspace --all-targets -- -D warnings` limpo; `check-core-purity.ps1` verde; `tsc --noEmit` + `npm run build` verdes | lacunas: provedor real com streaming não exercitado em E2E (só o FakeProvider em unit/integration); catálogo exercitado só via composition (não E2E puro)| crates/e2e/tests/e2e_files_read.rs::files_read_e2e_through_chat_orchestrator, crates/e2e/tests/e2e_degradation_declared.rs::degradation_declared_rejects_docs_generate_without_invoker | cargo test --workspace, cargo test --workspace |
| 3 | Motor de execução e ferramentas | concluída | **Etapas 1, 2, 3, 4, 4.x, 4.x.y, 5, 5.x, 4.1, 6.1 e 6.2 fechadas (Fase 3 completa).** **Etapa 4** (RunExecutor fecha o loop tool_call — "Fluxo vertical 1"): `frederico-execution-engine` no workspace (`unsafe_code = "forbid"`). `RunExecutor` consome `BoxStream<StreamEvent>`, valida via `validate_tool_call` (10 passos), executa via `Tool::execute`, emite `StreamEvent::ToolResult { id, ok, output }` (persistido em `message_events` com `kind = 'tool_result'`), adiciona ao contexto via `ChatMessage::tool(name, content, tool_call_id)` e volta a chamar o modelo. Loop até `Done`/`Error`/`Cancelled` ou budget estourado. `BudgetEnforcer` aplica `max_steps`. Mudanças no `provider-engine`: `Role::Tool` + `tool_call_id`. Migração `0004_tool_result_kind.sql`. **Etapa 4.x** (cancelamento + recovery): `CancellationToken` checado em 3 pontos do loop (entre rounds, entre eventos, e no `StreamEvent::Cancelled`). 2 testes em `tests/cancel.rs` (`cancel_via_provider_cancelled_event`, `cancel_via_token_before_first_tick`); 4 testes em `tests/recovery.rs` (replicam "Journal-then-emit"); `tests/common/mod.rs` extraído. **Etapa 4.x.y** (reorganizar crates — `ChatOrchestrator` movido pro `execution-engine` que delega o loop pro `RunExecutor`): resolve o ciclo de dependência (`provider-engine` re-exportava o `ChatOrchestrator` que dependia do `RunExecutor` que dependia do `provider-engine`). Grafo agora é `provider-engine ──► execution-engine ──► provider-engine` (sem ciclo). `provider-engine::orchestrator` mantém só `error_to_view` + `ProviderErrorView` + `OrchestratorError`/`OrchestratorResult` definidos **localmente**. `ChatOrchestrator::new` ganhou 4 args novos (`tool_registry`, `jail`, `tools`, `allowed_for_run`). `send_message` agora delega o loop pro `RunExecutor` via `tokio::spawn`. Cálculo de custo real reintroduzido via `descriptor.cost_microcents(p, c)` + `MessageRepo::set_usage_and_cost(p, c, cost)` + `ConversationRepo::add_cost(conv_id, cost)` quando `Completed`. 5 tests migraram do `provider-engine::orchestrator::tests` pro `execution-engine::tests::integration_orchestrator.rs`. Casca Tauri constrói o `ChatOrchestrator` com o tooling inicial (`ToolRegistry` + `Jail` no `current_dir` + `FilesReadTool` + `[ToolId::new("files.read")]`). **Etapa 5** (watchdog de 60s — escopo mínimo): `tokio::select!` no loop interno com 3 braços (`cancel.cancelled()` biased, `tokio::time::sleep(event_timeout)` watchdog, `stream.next()`). `event_timeout` configurável via `RunExecutor::with_event_timeout(Duration)` (encadeável; default 60s). Watchdog estoura → `MessageStatus::Timeout` / `RunStatus::Timeout` / `RunState::Interrupted` (a view `runs_with_status` mapeia `interrupted → timeout`). 1 teste em `tests/watchdog.rs` com `TrailingAdapter` que emite 1 delta e trava. **Etapa 5.x** (cancel real do HTTP + recovery de crash + `BEGIN IMMEDIATE` + state granular + Passo 10 auditoria — tudo numa única sub-etapa): **5.x.1 — Cancel real do HTTP**: o `tokio::select!` com `cancel.cancelled()` no `OpenAiCompatAdapter::stream` (e `AnthropicAdapter::stream`) JÁ EXISTIA desde antes; o teste E2E em `crates/provider-engine/tests/cancel_http.rs` (`cancel_drops_reqwest_connection` + `stream_completes_normally_when_not_cancelled`) prova agora que a stack é full-stack — o cancel fecha a conexão TCP, o reqwest aborta, o server detecta o EOF. 2 tests novos verdes. **5.x.4 + 5.x.3 — `RunState` granular a partir do journal + `BEGIN IMMEDIATE`**: novo módulo `state_mapping` (no `execution-engine`, não no `agent-engine` pra evitar ciclo) com `pub fn run_state_for_event(&StreamEvent) -> Option<RunState>` que mapeia cada evento (Delta → Streaming, ToolCall → WaitingToolCall, ToolResult → ValidatingToolResult, Done{Stop,Length} → Completed, Done{ToolCalls} → WaitingToolCall, Done{Error} → Failed, Error → Failed, Cancelled → Cancelled; Usage → None = sem mudança). 10 unit tests cobrem o mapping. A cada evento persistido pelo executor, se `run_state_for_event` devolve `Some`, chama `RunRepo::set_state_and_heartbeat_tx(run_id, state, last_event_seq)` numa **única transação** `BEGIN IMMEDIATE; ...; COMMIT;` que atualiza `state` + `last_heartbeat_at` + `last_event_seq` atomicamente (a Etapa 4 só chamava `set_status` que não é transacional). Novo método no `RunRepo`: `set_state`, `set_state_and_heartbeat_tx`, `list_stale_heartbeats`, `mark_interrupted`, `force_heartbeat_at_for_test`. **5.x.2 — Recovery de crash no startup**: novo módulo `recovery` (no `execution-engine`) com `recover_stale_runs` (lista runs não-terminais com `last_heartbeat_at` mais velho que `threshold` e marca como `interrupted` — a view `runs_with_status` mapeia `interrupted → timeout`) e `spawn_recover_stale_runs` (helper que recebe `Database` e constrói o `RunRepo` dentro do closure, sem `unsafe` — necessário porque a casca Tauri proíbe unsafe e o `RunRepo` tem borrow do `Database`). Threshold default `DEFAULT_STALE_THRESHOLD_SECS = 120` (maior que o `event_timeout` de 60s). Hook no `apps/desktop/src-tauri/src/main.rs::setup` chama `spawn_recover_stale_runs(db.clone(), Duration::from_secs(120))` em background (não bloqueia o startup). 2 tests em `tests/recovery.rs` (renomeado de `recovery` no `execution-engine/tests`; `recover_marks_only_stale_runs` + `recover_skips_terminal_runs`). **5.x.5 — Passo 10 de auditoria**: migration `0005_tool_audit.sql` cria tabela `tool_audit(id, run_id, tool_id, tool_version, arguments_json, result_ok, result_json, duration_micros, created_at)` (FK pra `runs` com `ON DELETE CASCADE`, índices `idx_tool_audit_run_created` e `idx_tool_audit_tool_created`). Append-only — sem `UPDATE` nem `DELETE` (mitiga R1 do `security-threat-model.md`). Novo `ToolAuditRepo` no `storage` (`append` + `list_for_run`). Novo trait `AuditSink` no `tool-registry` (`record(&self, entry) -> Result<(), String>`) com `NoopAuditSink` default e `RecordingAuditSink` pra testes. Novo `DbAuditSink` no `execution-engine::audit_sink` que implementa `AuditSink` via `ToolAuditRepo::append` (best effort — falha de I/O é logada via `tracing::warn!` mas não aborta a execução, conforme o spec do Passo 10). `RunExecutor::new` ganhou campo `audit_sink: Arc<dyn AuditSink>` (default `NoopAuditSink`); novo builder encadeável `RunExecutor::with_audit_sink(Arc<dyn AuditSink>)`. O `ChatOrchestrator::send_message` injeta `DbAuditSink::new(db.clone(), run_id)` ao montar o executor. O `RunExecutor::handle_tool_call` chama `audit_sink.record(AuditEntry { ... })` em todos os caminhos (aprovado+executado, rejeitado pelo validador) com `result_ok`, `arguments_json` raw, `result_json` serializado, e `duration` (medida com `Instant::now` antes/depois). 1 teste E2E em `tests/audit.rs` (`audit_records_files_read_execution`) valida que uma tool_call `files.read` aprovada+executada grava 1 entrada em `tool_audit` com `result_ok=true` e `arguments_json` correto. **Suíte do crate** `frederico-execution-engine`: **32/32 verde** (13 unit + 19 E2E = 10 state_mapping + 1 budget + 2 recovery + 6 e2e + 2 cancel + 4 recovery + 1 watchdog + 5 integration_orchestrator + 1 audit). **Suíte workspace 273/273 verde** em 35 targets (era 266 da Etapa 5.x; +3 accumulator + 1 anthropic tool_result) (era 252 da Etapa 4.x.y; +10 state_mapping + 2 recovery + 1 audit + 2 cancel_http no provider-engine + 1 cancel_http sanity). `cargo fmt --all -- --check` limpo; `cargo clippy --workspace --all-targets -- -D warnings` limpo; `check-core-purity.ps1` verde; `cargo tauri build` empacota NSIS `Frederico IA Studio_0.1.0_x64-setup.exe` em 2m 28s. `docs/modules/execution-engine.md` criado (RunExecutor, decisões não óbvias, API pública, cobertura de testes, diagrama de encaixe na arquitetura) e atualizado com as seções das Etapas 4.x (cancelamento + recovery), 4.x.y (reorganização dos crates), 5 (watchdog) e 5.x (5 sub-etapas). **Etapa 6.2 (frontend React)**: novo service `services/approvals.ts` (consome `AppOp::ApprovalList`/`Respond`); novo componente `components/ApprovalModal.tsx` (modal global com path, arguments em `<details>`, scope `Once`/`Run`/`Project`, reason opcional, badges Aprovar/Negar, badge `mandatory` quando for o caso). `App.tsx` polea `listApprovals()` a cada 2s via `setInterval` e renderiza o modal quando há entry pending. Estilo novo em `styles.css` (`.modal-backdrop`, `.modal`, `.approval-args`, `.btn-primary/danger`, `.badge/-warn`). `npm run build` verde — 49 modules, CSS 8.12 kB, JS 182.94 kB, build em 1.65s. **Fase 3 fechada em produção.** **Próximas etapas (da Fase 3):** **Etapa 6** (UI de aprovação com conteúdo real do `ApprovalRequest` + `PermissionSet` real do assistente/projeto + E2E em `tests/e2e/` com `files.read` de ponta a ponta — UI + Tauri + adapter real). | lacunas: cancelamento, recovery, approval, reload durante execução §32 não exercitados em E2E (só em integration tests no execution-engine/tests/); tornam-se trabalho da Fase 6| crates/e2e/tests/e2e_files_read.rs::files_read_e2e_through_chat_orchestrator, crates/e2e/tests/e2e_degradation_declared.rs::degradation_declared_rejects_docs_generate_without_invoker | cargo test --workspace, cargo test --workspace |
| 4 | Memória e continuidade | concluída | **Etapas 1, 2, 3, 4, 5 e 6 fechadas** (fundação + retrieval híbrido com embeddings + classificador LLM pós-resposta + fluxo de correção/expiração/retomada + UI do painel de memória com `ScoreBreakdown` visível). 5 ADRs (0010-0014) + 2 specs aprofundados + novo crate `frederico-memory` (puro, `unsafe_code = "forbid"`) com 9 arquivos Rust. **Etapa 1**: `MemoryRepo` com 11 métodos (`insert_auto_captured` rejeita `ExternalContent`; `search_lexical` FTS5 com `OR` explícito + expansão de escopo; `mark_superseded` idempotente; `purge_expired` deleta soft-deleted + superseded + expirados-não-pinned). Migration `0007_memory.sql`: 5 tabelas (`memory_records` 24 colunas, `memory_fts` FTS5 + 3 triggers, `memory_embeddings` PK `(memory_id, provider, model)`, `embedding_reindex_jobs`). `LexicalRetriever` (BM25 + recência + importance + confirmation). `NoopEmbeddingAdapter` + `NoopMemoryClassifier`. **Etapa 2**: `OpenRouterEmbeddingAdapter` (HTTP OpenAI-compat, default OpenRouter + `text-embedding-3-small`, 1536 dim, auth via `SecretString`/DPAPI, timeout 1.5s); `embedding_codec.rs` (encode/decode f32 little-endian + `cosine_similarity`); `EmbeddingWorker` (tokio task em background, batch 50, idempotente, sem retry em loop); `HybridRetriever` (5 sinais do `ADR-0011 §4`: lexical + recency + semantic + importance + confirmation, timeout total 2s, fallback lexical se provider indisponível, `cosine` clipado em ≥ 0); `RETRIEVER_BUDGET = 2s` const. **Gold-set** (10 cenários) + **Runner** (`tests/evaluation.rs`) roda **2 testes E2E**: `run_gold_set_evaluation` (baseline lexical, 10/10 F1=1.0) + `run_gold_set_evaluation_hybrid` (híbrido com `FakeHashEmbed`, 10/10 F1=1.0, gate `F1_híbrido ≥ 0.9` → não regride o baseline). Vazamento cruzado 0 (I4 atendido). p99 ~1ms. **`Database::open_in_memory()`** no storage (max_connections=1, pro runner ser determinístico). **`scoring.toml` + `eval.toml`** versionados. **`sanitize_fts5_query`** neutraliza caracteres perigosos + junta tokens com `OR` (FTS5 padrão usa AND implícito, matava BM25 parcial). **Decisão de empacotamento:** `Clock` continua no `frederico-security` (mover pro `core` é trabalho futuro, ADR-0014). **Etapa 3**: `CompletionProvider` trait + `NoopCompletionProvider` (porta fina pro `provider-engine` sem dependência cíclica) + `LlmMemoryClassifier` (LLM-based, default `openai/gpt-4o-mini` via OpenRouter, prompt restrito, parse de JSON, validação de scope/confidence/importance, threshold 0.6 default, cota 5/min via `Mutex<Vec<Instant>>` — regra do `ADR-0012 §2`). `MemoryExtractionJob` (run_id, conversation_id, messages, finished_at) + `MemoryExtractor` + `MemoryExtractorHandle` (worker pós-resposta, `tokio::spawn` em background, canal mpsc 256, sem `tokio::time::interval` — ADR-0014 §1). `determine_real_origin` helper (regra do `ADR-0012 §3`): sobrescreve o `origin` proposto pelo LLM com base na proveniência real das mensagens — se qualquer tem `source` começando com `tool_output:` ou `document_attachment:`, vira `ExternalContent` e vai pra `insert_pending_review` (UI da Etapa 5 consome a fila); se há mensagem `assistant` ou `tool`, vira `Assistant` (consolidação do modelo); senão, segue o que o LLM propôs (default `User`). Mitiga **E2** do security-threat-model.md em camada dupla (classificador sugere + worker decide). 7 testes novos do `MemoryExtractor` (User→auto_captured, external→pending_review, assistant→Assistant, classifier=None não insere, classifier=Err não panica, NoopMemoryClassifier compat, enqueue não-bloqueante). Renomeia `WorkerCommand`→`EmbeddingCommand` e `WorkerTask`→`EmbeddingTask` (coexiste com `ExtractorCommand`/`ExtractorTask`). `lib.rs` re-exports `MemoryExtractionJob, MemoryExtractor, MemoryExtractorHandle`. 6 testes do `LlmMemoryClassifier` no `classifier.rs` (NoopCompletion, parse válido, threshold baixo, JSON inválido, quota estourada). **Etapa 4**: `MemoryRepo::apply_correction(old_id, replacement, now) -> CorrectionResult` — fluxo atômico de "corrija para X" (`ADR-0014 §2`): valida replacement → BEGIN IMMEDIATE → insere a nova (com `user_confirmed = true`, `pending_review = false`) → marca a antiga como superseded (idempotente) → COMMIT. Devolve `CorrectionResult { old_id, new_record, superseded_at }` pro caller (UI da Etapa 5 mostra "essa memória foi substituída por X há 3 dias"). `MemoryRepo::list_pending_review(now)` — fila de revisão de ExternalContent (consumida pelo painel). Wire do `purge_expired` na inicialização (casca Tauri, `main.rs::setup`, best-effort). Regra "`user_pinned` bypassa `expires_at` mas não `superseded_by`" enforçada nas queries do `list_by_scope`, `list_pending_review` e `purge_expired`. `frederico-memory` + `chrono` adicionados como deps do `frederico-desktop`. 9 testes E2E novos em `crates/memory/tests/expiration.rs`. **Etapa 5**: IPC backend (`shared-contracts` + `main.rs`) ganha 6 `AppOp` (`MemoryList`, `MemoryRetrieve`, `MemoryApplyCorrection`, `MemoryConfirmPending`, `MemoryRejectPending`, `MemoryPurgeExpired`) + 4 views (`MemoryView`, `ScoreBreakdownView`, `MemoryHitView`, `CorrectionResultView`) + 1 input (`NewMemoryInputView`). `MemoryRepo::list_pending_review` consumido pelo IPC. 9 testes E2E roundtrip + 3 testes de view no `shared-contracts` (17/17 verde). `MemoryExtractor` wire no `ChatOrchestrator` (Etapa 3 → Etapa 5 fechada): campo `memory_extractor: Option<Arc<MemoryExtractorHandle>>` + helper `build_extraction_job` carrega as últimas 6 mensagens da conversa e enfileira um `MemoryExtractionJob` após cada Run finalizar (Completed ou Failed). Casca Tauri cria o `LlmMemoryClassifier` (com `NoopCompletionProvider` por enquanto — a Etapa 5.x injeta o `OpenRouterCompletionProvider` real) + `MemoryExtractor` em background, e passa o handle pro `ChatOrchestrator::new` (11º argumento). Frontend React: novo `services/memory.ts` (camada IPC, regra do `ADR-0003`), novo `components/MemoryPanel.tsx` (lista com `ScoreBreakdown` visível — `PROMPT MESTRE` §10.11 — + badges pinned/pendente/expirada/superseded + form inline de "corrija para X" + banner "Memória substituída" com link), nova rota `/memories` em `routes/Memories.tsx`, link "Memórias" no nav do `App.tsx`. `tsc --noEmit` + `npm run build` verdes — 52 modules (era 49; +3), CSS 10.22 kB (era 8.12 kB; +2.1 kB), JS 191.62 kB (era 182.94 kB; +8.7 kB), build em 624ms. **Etapa 6** (gate de CI): `tests/fixtures/gold_set.jsonl` expandido de 10 → 21 cenários cobrindo as 17 categorias do plano de avaliação (escopo project/client, correção de info antiga, expiração com pinned bypass, vazamento cruzado entre projetos, sinônimos, cross-language, prompt injection como dado, malicious memory, documento anexo com payload → `pending_review`, duplicate conflict, no_results, greeting curto, message_ok). `config/eval.toml` com alvos de produção (hard fail em CI): precisão ≥ 0.70, F1 ≥ 0.65, vazamento cruzado = 0 (I4), p99 ≤ 2000ms (§10.13), p95 ≤ 1000ms (warning). `tests/evaluation.rs` runner agora carrega o `eval.toml` em tempo de teste (`load_eval_gate` parse manual — evita dep nova de `toml`); corrige bug do `run_scenario` que usava `insert_auto_captured` direto (agora escolhe baseado em `user_confirmed` + `origin`). `Default for EvalGate` agora reflete os alvos da Etapa 6 (era Etapa 1 permissivo). Medido: P 0.905, F1 0.937, vaza 0, p99 1-3ms (gate 2000ms — folga de 3 ordens de grandeza). 21/21 verde nos 2 testes do runner. Suíte workspace: 374/374 verde (era 365; +9 do shared-contracts). `cargo fmt`, `cargo clippy --workspace --all-targets -- -D warnings`, `check-core-purity.ps1` todos limpos **Consolidada no `main` pelo commit de merge `91fdd53`** (PR #1, `fase-4/etapa-1-fundacao`, 16 commits). Specs `memory-architecture.md` e `memory-evaluation-plan.md` carimbados `parcialmente implementado` em 2026-07-28; `CHANGELOG.md` atualizado. | lacuna: e2e_memory_real_embeddings_recall_by_paraphrase é só noturno (#[ignore], depende de OPENROUTER_API_KEY); adaptador real presumido, não provado — a Fase 6 ou a próxima etapa que configurar a secret noturna vira essa lacuna em cobertura| crates/e2e/tests/e2e_memory_pipeline_end_to_end.rs::e2e_memory_pipeline_end_to_end_deterministic | cargo test --workspace |
| 5 | Documentos | concluída | **Fase 5 fechada (8 PRs merged em main: #11 Etapa 2B, #12 Etapa 2B+Y, #13 Etapa 3, #14 Etapa 4, #15 Etapa 5 PR 1 skeleton + ADR-0021, #17 Etapa 5 PR 2 PDFPro v0.1 real, #19 Etapa 5 PR 3 auditoria estrutural bloqueante do §19.4)**. `document-worker` v0.4.0 com 8 handlers (docx.write/read, xlsx.write/read, pdf.write/read/audit, ocr.run); `frederico-document-kits` v0.1 com 3 kits reais (WordPro, ExcelPro, PdfPro) — `KitRegistry::implemented_formats() == ["docx", "xlsx", "pdf"]`. **Etapa 5 PR 2 (PR #17, `7fedf19`)**: `PdfProKit::render` real (reportlab Platypus + fontes Tinta & Latão embutidas D-FAIL-1 + identidade visual + modo Sóbrio + 20 blocos cobertos) + glifo-check pre-render via fontTools (D-GLYPH-1) + marca d'água opt-in (D-PDF2) + bump atômico do enum no mesmo commit (precedente ADR-0020 §3 D3). **Etapa 5 PR 3 (PR #19, `0e3471f`)**: `pdf.audit` capability no `document-worker` v0.4.0 (D-PDF5 + D-PDF6) — 5 checks baseline (abertura, n_pages ≥ 1, DocInfo populado, fontes embutidas, sem cifragem/referências externas) + 4 A-2B opt-in (OutputIntent com sRGB ICC v2 embedded, XMP `pdfaid:part=2` + `pdfaid:conformance=B`, sem JavaScript). `KitError::AuditFailed` novo variant no `kit.rs`; `PdfProKit::render` chama `pdf.audit` após `pdf.write`; `DocsGenerateTool` (generate.rs) formata a mensagem pro modelo. `SpecVersion` 0.2.0 → 0.3.0 (MINOR, `DocumentMetadata.pdfa: Option<PdfaSpec>`). sRGB ICC v2 (D-PDF6) gerado pelo `tools/generate_srgb_icc.py` (526 bytes, SHA-256 `C4188E5C06585DDAD4F8781B8F8791BB4563874AC462C934D1420EA133D74B64` pinado em `bootstrap.ps1`). **Tagged PDF NÃO é verificado** — A-2B (básico) não exige Tagged; v1 declara apenas nível B; PDF/A-2A (Tagged) é não-objetivo declarado. Suíte workspace verde (PR #19 CI run `30758984691` SUCCESS, ~412+ testes Rust + 19/19 Python do `pdf.audit` + 3/3 E2E do `pdf.write`); `cargo fmt --check` limpo; `cargo clippy --workspace --all-targets -- -D warnings -D clippy::await_holding_lock` limpo; `node scripts/check-docs.mjs` OK; `./scripts/check-core-purity.ps1` OK. Specs `pdfpro-specification.md`, `excelpro-specification.md`, `process-architecture.md` (carimbo 2026-08-01) e `docs/modules/document-kits.md` (carimbo 2026-08-01) promovidos pra `parcialmente implementado` (sem `implementado` porque há pendências 5.x nomeadas — vide coluna ao lado). CHANGELOG.md atualizado com a entrada do PR 19. Saga da PR #16 (mergeada em branch errada) resolvida via PR #19 com `git checkout <SHA-delta> -- .` workaround. Saga do PR #7 (Etapa 2A — deadlock `Arc<Mutex<Box<dyn Pipe>>>`) fechada com redesign do `WorkerManager` como ator (ADRs 0015 + 0016). Saga do `5c39bac` (PR 1 — bump decorativo sem render) revertido no PR 1 merge commit, e o bump atômico correto do `DocumentFormat::Pdf` entrou no PR #17. Saga do `sRGB2014.icc` (color.org 404) resolvida com gerador local via IEC 61966-2-1:1999 + ISO 15076-1. Saga do `e2e_pdf_write_and_read` (formato antigo `sections`) resolvida com adaptação pra `blocks` (PR #17 commit `c11c39b`). Saga do `cargo fmt` (rustfmt CI mais novo que local) resolvida com `cargo fmt --all` (PR #19 commit `9c31fcb`). CI noturno (`ci-nightly.yml`) com Tesseract 5.4 + tessdata continua rodando pra veraPDF validar A-2B no job separado. 8 pendências 5.x nomeadas (vide coluna ao lado) — vira trabalho da próxima fase/etapa, não bloqueiam a promoção (regra de promoção em `status.md` §Regra de promoção exige suíte verde + specs parcialmente/implementado + CHANGELOG + referência ao PR — todos atendidos). | lacunas: xlsx/pdf com Python real atravessando composição não cobertos (só docx); e2e_docs_generate_with_real_worker é só noturno (#[ignore]) — coberto em cargo test --workspace pelo e2e_docs_generate_with_fake_worker (twin determinístico, ADR-0026 D2)| crates/e2e/tests/e2e_docs_generate_with_fake_worker.rs::docs_generate_with_fake_worker_closes_run_completed, crates/e2e/tests/e2e_docs_generate_with_real_worker.rs::docs_generate_with_real_worker_produces_valid_docx | cargo test --workspace, E2E document-worker handlers |
| 5b | Fase de Ligação (integração casca + document-kits no ToolRegistry) | concluída | **6 de 6 etapas fechadas (PRs #21, #22, #23, #24 + **#25 merged em main — Etapa 5.X patch-allowed-paths**):** Etapa 1 — `frederico-app` (camada de composição pura) + `JailResolver` + `build_chat_orchestrator` consumido pela casca (PR #21, ADR-0022). Etapa 2.A — `DocumentWorkerLauncher` (lazy + restart on death com teto + kill tree) + 3 `tauri::command` (`DocumentWorkerStatus` / `DocumentWorkerInvoke` / `DocumentWorkerReset`) (PR #22, ADR-0023). Etapa 2.B — trait `WorkerInvoker` no `core` (ADR-0024) + `InvokeError` próprio + bump atômico do `documents: None → Full` + integração dos 3 kits no `ToolRegistry` (PR #23). **Etapa 5** — nova crate `frederico-e2e` (14º membro do workspace, `publish = false`, path fixado em `crates/e2e/tests/`) com 5 testes E2E atravessando o caminho de produção **sem subir a casca Tauri** (consomem `frederico_app::build_chat_orchestrator` direto, mesma função da casca — regra do ADR-0022 §D4): `e2e_files_read` (caminho do `files.read`), `e2e_degradation_declared` (sem invoker → catálogo só `files.read`; provedor tenta `docs.generate` → `RunExecutor` rejeita via `UnknownTool` → run `Failed` com `Message.error` claro), `e2e_jail_per_conversation` (2 conversas, `Jail::resolve` rejeita `../<cid_b>/secret.txt`), `e2e_docs_generate_with_fake_worker` (`WordProKit::render` chama `WorkerInvoker::invoke` via `FakeWorker` in-process), `e2e_docs_generate_with_real_worker` (#[ignore]; gera `.docx` real via `DocumentWorkerLauncher` Python — **único teste que vai até o fim do caminho do produto**). Helper `build_orchestrator` em `tests/common/mod.rs` é o ponto único de montagem. `docs/architecture/testing-strategy.md` promovido a `parcialmente implementado` com 2 adições (a fronteira do que os E2E cobrem + a regra da composição compartilhada). `docs/modules/e2e.md` criado (§1.4 da REGRAS). `verify-external.ps1` Step 7: roda o teste de worker real com `--include-ignored` depois do `bootstrap.ps1`. Suíte workspace 533→538+ testes verde (4/5 E2E sem runtime, 5/5 com runtime); `cargo fmt --check` limpo; `cargo clippy --workspace --all-targets -- -D warnings -D clippy::await_holding_lock` limpo; `check-core-purity.ps1` OK; `check-docs.mjs` OK; `check-fase-5-untouched.ps1` OK. **Etapa 5.X — patch-allowed-paths** (PR #25): bug latente do `WorkerToolDispatcher` desde a Etapa 3 da Fase 5 — vetor vazio no construtor + `validate_against_allowlist` fail-open em `use_canonical=false` (verbatim `\\?\` não strippado do `canonical`, só do `allowed` — `starts_with` falhava mesmo o path dentro do jail) — barreira desligada **e** quebrada no caminho de produção do `docs.generate`. Fix em 4 commits: (1) `validate_against_allowlist` nega allowlist vazia (fail-closed), (2) `WorkerToolDispatcher::new(invoker)` recebe allowlist por chamada + `Jail::root_canonical` getter + `DocsGenerateTool::execute` usa `Jail::resolve_allowing_nonexistent` como barreira primária + mitigação symlink explícita (rótulo "parcial", TOCTOU documentado) + `check_path` fail-closed como defesa em profundidade + `DocsInspectTool::execute` mesma estrutura com `Jail::resolve` (lê arquivo existente) + bug do verbatim consertado, (3) `path_safety` integration test com 6 cenários no `document-kits` (allow relativo, reject absoluto, reject traversal, allow mixed-case, reject symlink, fail-closed), (4) bump atômico. **6 de 6 etapas da Fase de Ligação fechadas.** Pendência 1 do `process-architecture.md` (path safety forte) **fechada**; pendência 5 nova (escrita segura no worker Python — `O_NOFOLLOW` / `O_CREAT|O_EXCL` no `open` dos handlers). `docs.inspect` **mudou de comportamento visível**: agora recusa inspecionar arquivo fora do workspace da conversa (antes a barreira era no-op). Detalhes + lição aprendida (comentário aspiracional no teste vs. comentário ancorado em teste) na narrativa do PR. | Etapas 3, 4 e 6 (listadas como pendentes na versão anterior deste `status.md`) foram fechadas por este PR (#28) e PRs anteriores (#26, #27). Pendência 5 do `process-architecture.md` (escrita segura no worker Python) vira trabalho de fase futura. **Etapas concluídas neste PR + anteriores:** **Etapa 3** — `MemoryExtractor` + embedding adapter reais (só depende de Fase 4 Etapa 5, já fechada). **Etapa 4** — decidir `frederico-agent-engine` (pendência de Fase 6). **Etapa 6** — regra de "definição de pronto" + gate CI que valida "E2E que atravessa a casca" por fase (vai procurar por `crates/e2e/tests/` — **path fixado**, não mover sem ADR). |e Etapa 1). **Etapa 4** — decidir `frederico-agent-engine` (nenhuma dependência). **Etapa 6** — regra de "definição de pronto" + gate CI. **Etapa 7 (`SecurityJailResolver` modo desenvolvedor) removida** — depende da Fase 7 do PROMPT MESTRE; uma fase de ligação que depende de fase futura nunca fecha. Virou pendência nomeada dentro da Fase 7 (criar `SecurityJailResolver` em `crates/security/src/jail.rs` + substituir `FileSystemJailResolver` no `setup` da casca + wire do kill-tree via Job Objects do Windows). | crates/e2e/tests/e2e_files_read.rs::files_read_e2e_through_chat_orchestrator, crates/e2e/tests/e2e_degradation_declared.rs::degradation_declared_rejects_docs_generate_without_invoker, crates/e2e/tests/e2e_jail_per_conversation.rs::jail_per_conversation_blocks_path_traversal, crates/e2e/tests/e2e_docs_generate_with_fake_worker.rs::docs_generate_with_fake_worker_closes_run_completed, crates/e2e/tests/e2e_docs_generate_with_real_worker.rs::docs_generate_with_real_worker_produces_valid_docx, crates/e2e/tests/e2e_memory_pipeline_end_to_end.rs::e2e_memory_pipeline_end_to_end_deterministic, crates/e2e/tests/e2e_memory_real_embeddings.rs::e2e_memory_real_embeddings_recall_by_paraphrase | cargo test --workspace, cargo test --workspace, cargo test --workspace, cargo test --workspace, E2E document-worker handlers, cargo test --workspace, E2E memory real (OpenRouter + gpt-4o-mini + text-embedding-3-small) |
| 6 | Multimodelo e subagentes | concluída | Etapa 1 (planejamento) fechada em PR #29: specs `multimodel-architecture.md` e `subagent-architecture.md` aprofundados (v1 — antes stubs da Fase 0); 4 ADRs novos (0027 budget+anti-explosão, 0028 pipeline-only, 0029 RunEvent journal, 0030 SpecialistRegistry). **Etapa 2 (portão único de transição + RunEvent journal) fechada em PR #30**: `state_mapping::run_state_for_event` agora retorna `Result<Vec<RunStateTransition>, TransitionError>` (caminho completo de 1-3 arestas, sem compressão), `RunExecutor` consome o portão via `apply_transition_check` e grava cada transição como `RunEvent` separado, 4 novas arestas adicionadas à tabela (23-26: `CallingModel + ToolCallEmitted → WaitingToolCall`, `ContinuingModel + MessageComplete → Checkpointing`, `ContinuingModel + FirstToken → Streaming`, `WaitingToolCall + ResultValid → ContinuingModel`). Nova tabela `run_events` (UNIQUE `run_id, seq`, FK `run_id` com `ON DELETE CASCADE`) + coluna `run_seq` em `message_events` (join explícito `message_events.run_seq = run_events.seq` per ADR-0029 §D6) via migração `0027_run_events.sql`. `RunEventRepo` em `crates/storage` (5 métodos: `append`, `list_for_run`, `latest_for_run`, `last_seq`, `set_run_seq`). `recovery.rs` lê `RunEvent.latest_for_run(run_id).to_state` como fonte primária (não o `runs.state` legado). `orchestrator` seta `CallingModel` antes de chamar o executor (transição `Created → CallingModel` é responsabilidade do orchestrator, não do executor). Specs `multimodel-architecture.md` e `subagent-architecture.md` promovidos a `parcialmente implementado`. Mapa de E2E planejado para as Etapas 3-6 declarado nos specs §"E2E de cobertura planejado por etapa" (todos em `crates/e2e/tests/`, consumindo `build_chat_orchestrator` — caminho de produção, não de crate). **Etapa 3 PR 1 (SpecialistRegistry, [ADR-0030](decisions/0030-specialist-registry-from-model-catalog.md) §D1+D2+D4+D5) fechada em PR 1 da Etapa 3**: 8 `SpecialistDefinition` bundled em `crates/model-catalog/data/specialists/default.toml` (revisor, pesquisador, testador, validador, sumador, arquiteto, crítico, executor) embedded via `build.rs` (mesma estratégia do `data/catalog.json`). Trait `SpecialistRegistry` (`get`/`list`/`validate_id`) + `DefaultSpecialistRegistry` (bundled + override `~/.config/frederico/specialists.toml`, com regra "override não invade bundled" — bundled vence com warning explícito). `RegistryError::UnknownSpecialist` **sempre** carrega `valid: Vec<SpecialistId>` (zero fallback silencioso, §9.2). `frederico_app::composition::build_specialist_registry(catalog)` (factory) + `SpecialistBundle` (registry + catalog pareado). Tauri command `list_specialists` (consumido pelo `<SpecialistPicker>` React, sem spawn — a Etapa 3 não invoca subagente, então listar antes da interseção existir **não cria janela de privilégio** — `permission_loader` é o PR 2). `SpecialistSummary` resolve `default_model` → `capability_tags` via `Catalog::find_model` (UI pode renderizar badges sem consultar catálogo de novo). Override do usuário parse-falha → warning + fallback bundled (degradação declarada, mesma família do `OpenRouter API key ausente`). `directories = "5"` e `tracing` adicionados como deps do `model-catalog`. Suíte workspace: 24 testes novos do `model-catalog` (specialist + registry) + 2 E2E (`registry_loads_specialists_from_catalog` e `specialist_unknown_id_returns_structured_error` — ambos via `build_specialist_registry`, caminho de produção). | **Modos fora de escopo da Fase 6** ([ADR-0028 §D2](decisions/0028-pipeline-sequencial-multimodel.md)): Comparação, Conselho e Debate são modos documentados no spec `multimodel-architecture.md` §"Modos documentados e fora de escopo da Fase 6" e adiados para incremento posterior — **não são critério de aceite da Fase 6**. **Pendências herdadas de PRs anteriores, fechadas pela Etapa 2:** (i) portão único de transição ([ADR-0025 §D3](decisions/0025-agent-engine-decision.md) + ADR-0029 §D1) — fechado em PR #30; (ii) memory control na interface (CHANGELOG Etapa 3 Fase de Ligação) — pendente, planejado pra Etapa 3 ou 6. **Etapa 3 PR 2 (PermissionSet real do `assistant`/`project`/`user`, [ADR-0030](decisions/0030-specialist-registry-from-model-catalog.md) §D3) fechada em PR 2 da Etapa 3**: `PermissionSet::merge3` (interseção tripla, **fail-closed** — categoria ausente ou desconhecida em qualquer dos 3 layers **nega**, nunca herda; testado em 11 unit tests no `permission.rs` e 2 E2E no caminho de produção) + `PermissionLoader::load_effective_permission_set(user, project, assistant) -> PermissionSet` (cache em memória chaveado por `(path, content_hash)` — revogação de privilégio propaga via invalidação de hash; **cacheia parse, não decisão** — decisão é recomputada a cada chamada via `merge3` fail-closed, sem janela de privilégio obsoleto) + `build_default_permission_set` factory em `crates/app/src/composition.rs` (mesma função que a casca Tauri consome no setup) + migração `0028_profiles.sql` (tabela `permission_profiles` cacheando `content_toml` + `content_hash` + `schema_version` — schema bump invalida todos os profiles cached) + 2 E2E em `crates/e2e/tests/e2e_permission_loader_e2e.rs` (`permission_set_inherited_from_assistant_project_user` e `effective_permission_set_is_subset_of_parent` — o último prova o invariante triplo `effective ⊆ user ∧ effective ⊆ project ∧ effective ⊆ assistant` no caminho de produção, não só na função pura, conforme memory "Cobertura de invariante no caminho de produção"). `Default` adicionado em `FileReadPermission`/`RuntimePermission`/`TerminalMode`/`GitPermission`/`GitHubPermission`/`MemoryPermission`/`DocumentPermission` (todos variante `None` = deny) — pré-requisito pro `unwrap_or_default()` no `permission_loader` e fail-closed consistente. **`docs/modules/tool-registry.md §3`** perde a frase "a Etapa 4 carrega o `PermissionSet` real do `assistant`/`project`/`user` antes de validar — pendente" (pendência fechada). Suíte workspace: 11 testes novos do `PermissionSet::merge` + 9 do `PermissionLoader` + 2 E2E = 22 testes novos. Pendência (ii) memory control na interface ainda aberta. **Etapa 4 PR 1 (infra de subagente + invariantes do ADR-0027, sem spawn real ainda) fechada em PR 1 da Etapa 4**: 4 peças no `frederico-agent-engine` (puro, sem I/O) — (a) `BudgetAllocation` (subset do `Budget` que o pai libera pro filho, D5 do ADR-0027) com `BudgetAllocation::try_from(parent_remaining, requested)` fail-closed e `split(parts)` com resto no pai; (b) `Budget::try_allocate(parent_remaining, requested) -> Result<Budget, AllocationError>` constrói o `Budget` do filho como `min(parent_remaining, requested)` (D3 do ADR-0027: "filho não tem Budget próprio; tem uma janela"); (c) `SpentBudget` (cópia em memória do gasto efetivo) + `Budget::remaining(&spent) -> Budget` (descontando, não copiando); (d) `SubagentError` (6 variantes do D4: `GlobalLimitReached` + `DepthExceeded` + `AllocationExceedsParent` + `UnknownSpecialist` + `PermissionDenied` + `InternalError`) com `Display` legível pelo modelo (texto tem nome do eixo + valores + sugestão de ação) + `is_model_recoverable`; (e) `SubagentBudgetLedger` (livro de alocações vivo por `RunId`, função pura testável em isolamento) carrega o invariante de soma do D3 ("Σ alocações vivas ≤ pai.remaining_inicial − pai.gasto_atual") — `try_record` rejeita se a soma da nova + existentes excede o pai; `release` é idempotente. `Run` em memória ganha `subagent_count: u32` + `depth: u32` + `parent_run_id: Option<RunId>` + `spent: SpentBudget` + `new_subagent(parent)` constructor + `is_subagent` + `record_spent`. **Migração `0029_subagent_runs.sql`**: adiciona colunas em `runs` (subagent_count, depth, parent_run_id FK ON DELETE CASCADE, spent_microcents/tokens_in/tokens_out/steps) + nova tabela `subagent_runs` (PK = `runs.id` do subagente, parent_run_id FK, specialist_id, depth CHECK 1..2, allocation_json, spent_*, started_at, finished_at, state, `UNIQUE(parent_run_id, id)` anti-exploit) + 3 índices (`idx_runs_parent_run_id`, `idx_runs_depth`, `idx_subagent_runs_parent_active`). `RunRow` migra de tupla (16 cols) pra struct (`#[derive(sqlx::FromRow)]`, 23 cols — `sqlx::FromRow` só deriva direto pra tuplas até 9 elementos). `RunRepo` ganha 4 métodos novos: `increment_subagent_count` (UPDATE atômico + SELECT, transação), `decrement_subagent_count` (saturado em 0), `set_depth` + `set_parent_run_id` (atômico), `add_spent` (acumula `SpentBudget` no banco). Nova struct `SubagentRunRecord` + `SubagentRunRepo` (`record` / `complete` / `list_active_for_parent` / `row_to_record`). **Sem `SubagentRunner` ainda** — fica pra Etapa 4 PR 2 (`SubagentRunner` real consumindo `ChatOrchestrator` + `SpecialistRegistry` + `PermissionLoader` no caminho de produção) fechada em PR 2 da Etapa 4: 5 testes unit + 6 E2E no caminho de produção (`subagent_runs_with_reduced_permissions`, `subagent_inherits_cancellation_token`, `subagent_budget_discounted_from_parent_in_real_path`, `subagent_explosion_cap_8_rejects_ninth`, `subagent_depth_cap_2_rejects_grandchild`, `subagent_budget_sum_never_exceeds_parent`). Etapa 5 2/2 PRs fechadas (5 PR 1 + 5 PR 2); Etapa 6 (UI) fechada em PR XXX — 6/6 etapas fechadas (Pipeline Sequencial em produção, D5/D6/D7 do ADR-0028 entregues). Suíte workspace: 35 testes novos do `agent-engine` (12 BudgetAllocation + 5 SpentBudget + 5 Budget::remaining + 4 Budget::try_allocate + 6 AllocationError display + 6 SubagentError + 8 SubagentBudgetLedger incluindo o invariante do D3) + 18 do `frederico-agent-engine` existentes agora 86 no total (era 51). Verificações locais verdes (fmt, clippy, test --workspace, core-purity, e2e-gate, check-docs, typecheck). **Etapa 5 PR 1 (PipelineRepo infra, ADR-0028 D5/D6/D7) fechada em PR XXX**: 3 tabelas novas em `crates/storage/migrations/0030_multimodel.sql` (`multimodel_runs` cabeçalho, `multimodel_stages` sequência com `UNIQUE(run_id, seq)`, `multimodel_artifacts` arquivos produzidos — FKs com `ON DELETE CASCADE` no cabeçalho e `SET NULL` nos artefatos referenciados). Módulo `crates/storage/src/multimodel.rs` (~29KB) com `MultimodelMode` (Pipeline, mais 3 planejados pra Etapas futuras per ADR-0028 §D3), `MultimodelState` (Pending/Running/Completed/PartiallyCompleted/Failed/Cancelled — `PartiallyCompleted` é exclusivo do pipeline, suporta D5 do ADR-0028), `MultimodelArtifactKind` (Text/File/Json/Markdown), structs de domínio (`MultimodelRun`/`MultimodelStage`/`MultimodelArtifact`), `MultimodelError` (5 variantes: `RunNotFound`/`StageNotFound`/`ArtifactNotFound`/`DuplicateStage`/`InvalidValue`) e `PipelineRepo` com 12 métodos. `PipelineRepo::create_stage` rejeita UNIQUE (run_id, seq) com `MultimodelError::DuplicateStage` (anti-exploit do orchestrator). `PipelineRepo::list_reusable_stages(run_id, previous_output_hash)` é a primitiva do D6 (reuso quando input não muda). `PipelineRepo::list_resumable()` retorna `Running` + `PartiallyCompleted` ordenados por `updated_at DESC` (D5: oferece continuação no startup). `StorageError` ganha variante `Multimodel(#[from] MultimodelError)` (envelope pro caller usar `?` sem ter que lidar com o tipo interno). Re-exports seletivos no `lib.rs` (`MultimodelRun`/`MultimodelStage`/`MultimodelArtifact`/`MultimodelMode`/`MultimodelState`/`MultimodelArtifactKind`/`MultimodelError`/`PipelineRepo` + funções `new_run_id`/`new_stage_id`/`new_artifact_id`/`hash_file`). 5 E2E novos em `crates/e2e/tests/e2e_pipeline_sequencial_e2e.rs` cobrindo o caminho do `PipelineRepo` (roundtrip save/load de run+stage+artifact, listagem de resumable, reuso por hash, agregação de custo, integridade de UNIQUE). **Sem `MultimodelOrchestrator` ainda** — o runner que executa stages sequencialmente, propaga cancel e reusa stage é a **Etapa 5 PR 2**. Mesma fronteira do PR #25 e do PR #34: PR 1 entrega infra testável em isolamento, PR 2 fecha o orchestrator no caminho de produção. Suite workspace: 4 unit tests do `multimodel` (roundtrip enums + new_ids unique) + 5 E2E novos do `e2e_pipeline_sequencial_e2e` — total do `frederico-storage` agora 22 testes (era 18 antes da Etapa 4). Verificações locais verdes. **Etapa 5 PR 2 (MultimodelOrchestrator real consumindo `RunExecutor` no caminho de produção, ADR-0028 D5/D7) fechada em PR XXX**: módulo `crates/execution-engine/src/pipeline_orchestrator.rs` (~600 linhas) com o runner sequencial que consome o `PipelineRepo` da Etapa 5 PR 1. **2 métodos principais**: `start_pipeline(parent_run_id, Vec<StageSpec>) -> Result<MultimodelRunId, PipelineError>` (assíncrono via `tokio::spawn`; persiste `MultimodelRun` em `state=Running` ANTES de spawnar, retorna imediatamente o `pipeline_id` = `MultimodelRun.id`) e `cancel_pipeline(pipeline_id) -> Result<(), PipelineError>` (cascateia o `CancellationToken` pro `RunExecutor` do stage em curso; D7: stages futuros não iniciados marcados `Cancelled` direto, sem chamar o modelo). **Loop sequencial de stages** (pra cada `StageSpec`): 1) checa `is_cancelled()` no início (D7); 2) **D6 reuso desabilitado no PR 2** (a semântica do `list_reusable_stages` precisa ser revisada — Etapa 6 pluga); 3) cria `MultimodelStage` (state=pending) via `PipelineRepo::create_stage`; 4) cria `Message` (assistant) + `Run` (com `parent_run_id` do pipeline); 5) aplica as 5 arestas de inicialização (`Created → Queued → PreparingContext → RetrievingMemory → ValidatingCapabilities`, Etapa 2 da Fase 6) via `RunRepo::set_state_validated`; 6) busca adapter do `ProviderMap` e descriptor do `Catalog`; 7) monta `RunExecutor` (mesma configuração do `ChatOrchestrator::send_message` — `permission_set`, `allowed_for_run`, `tools`, `jail_resolver`); 8) chama `executor.run(msg_id, run_id, model_id, [ChatMessage::user(input)])` com o input do stage (encadeado com o output do anterior); 9) calcula `cost_microcents` via `descriptor.cost_microcents(p, c)`; 10) persiste `complete_stage` (state=completed/failed/cancelled, cost, output_artifact_id, output_hash) + `add_cost` no `MultimodelRun` + `create_artifact` (Text) com o output. **Estado final do pipeline**: `Cancelled` se D7 cascateou, `Completed` se todos os stages processados sem falha (`cost_microcents >= 0`), `PartiallyCompleted` caso contrário. **`PipelineError`** com 5 variantes: `RunNotFound`/`StageNotFound`/`ArtifactNotFound`/`DuplicateStage`/`InvalidValue` (re-exportado pra que callers usem `?` sem boxing). **Integração com `ChatOrchestrator`**: bump atômico de `ChatOrchestrator::new` (12 → 14 args, mesmo padrão do `subagent_runner` da Etapa 4 PR 2). `ChatOrchestrator` ganha 2 métodos de delegação: `start_pipeline(parent_run_id, stages) -> Result<MultimodelRunId, ChatOrchestratorError>` e `cancel_pipeline(pipeline_id) -> Result<(), ChatOrchestratorError>`. `ChatOrchestrator::multimodel_orchestrator: Option<Arc<MultimodelOrchestrator>>` (Option pra retrocompatibilidade com testes que constroem `parts` manualmente). `ChatOrchestratorError` ganha variante `Pipeline(#[from] PipelineError)`. `ChatOrchestratorParts` ganha `multimodel_orchestrator: Option<...>`. **Integração Tauri**: `apps/desktop/src-tauri/src/main.rs` constrói o `MultimodelOrchestrator` antes do `ChatOrchestrator` e injeta via `parts.multimodel_orchestrator = Some(...)`. 2 Tauri commands novos (`start_pipeline` + `cancel_pipeline`) são trabalho da Etapa 6 (UI) — o PR 2 entrega o backend; a UI consome via `ChatOrchestrator::start_pipeline` / `cancel_pipeline` direto. **5 E2E novos em `crates/e2e/tests/e2e_pipeline_sequencial_e2e_orchestrator.rs`** (complementam os 5 de `e2e_pipeline_sequencial_e2e.rs` da Etapa 5 PR 1 — esses cobrem persistência, o novo cobre execução real): `pipeline_two_stages_executes_via_orchestrator` (D5: 2 stages executam sequencialmente, `MultimodelRun.state = Completed`, `total_cost_microcents >= 0`, `call_count == 2`), `cancel_pipeline_not_found_returns_structured_error` (mecanismo: `cancel_pipeline` em ID inexistente retorna `Err(NotFound)` com `Display` legível), `start_pipeline_empty_stages_returns_error` (validação síncrona: `stages` vazio rejeita com `Err(ProviderFailed)`), `start_pipeline_unknown_provider_fails_at_stage` (validação assíncrona: provider inexistente → stage 1 `state=failed`, pipeline `PartiallyCompleted`), `pipeline_error_display_includes_context` (todos os variantes de `PipelineError` têm `Display` legível pelo modelo: nome + IDs + ação). **Documentação atualizada**: `docs/architecture/multimodel-architecture.md` carimbo atualizado pra `parcialmente implementado` 2026-08-07 (fase 6 com Etapa 1 + Etapa 2 + Etapa 5 PR 1 + Etapa 5 PR 2 fechadas; Etapa 6 pendente). **Verificações locais verdes**: fmt, clippy --workspace --all-targets, test workspace (storage 22/22, e2e 28/28, app, agent-engine, etc.), `check-core-purity.ps1`, `check-e2e-gate.ps1`, `check-docs.mjs`. **Sem D6 reuso efetivo ainda** (a semântica do `list_reusable_stages` precisa ser revisada: o ADR-0028 §D6 fala em `output_hash` matching, mas o `list_reusable_stages` busca no **stage atual** que está prestes a rodar, e esse ainda não tem output. A Etapa 6 (UI) re-implementa com a semântica correta: checa `input_hash` matching **antes** de criar o stage). **Suite workspace**: 5 E2E novos + 4 unit tests (do `multimodel`, Etapa 5 PR 1) — total do `frederico-e2e` agora 28 E2E (era 23 antes da Etapa 5 PR 1). **Etapa 7 (UI/Polish do Modo Equipe, ADR-0028 §D1 item 4) fechada em PR #40:** UI React do Modo Equipe (rota `/team`, `PipelineView` com sidebar de pipelines resumable + detalhe por stage + modal de criação). 4º Tauri command novo `list_pipeline_stages(run_id) -> Vec<MultimodelStage>` (necessário pra UI mostrar o progresso por stage — sem ele, a sidebar mostraria cabeçalhos sem detalhe). `services/pipelines.ts` consumido pela UI: `startPipeline` + `cancelPipeline` + `listResumablePipelines` + `listPipelineStages` (4 funções, todas wrappando `invoke()` direto dos Tauri commands dedicados — não via `ipc_dispatch`, mesma decisão dos 3 commands da Etapa 6). `components/PipelineView.tsx` (sidebar com polling 2s — mesmo padrão do `App.tsx` para a fila de aprovação — + lista de stages com `state`/`cost_microcents`/`input_hash`/`output_hash` + botão "Cancelar" se `state === "running"`). `components/PipelineCreateForm.tsx` (modal com seleção de `parent_run_id` via `listConversations` + N stages dinâmicos, provider/model/input por stage, default `simulated`/`simulated-echo` pra permitir testar sem configurar credencial real). Rota `/team` + link no nav do `App.tsx`. CSS novo em `styles.css` (`.pipeline-view`/`.pipeline-sidebar`/`.pipeline-stage*`). **Botão "Retomar" (D5) deixado como TODO** com tooltip explicando: retomar fielmente requer armazenar o `input` original de cada `MultimodelStage` no DB (mudança de schema, migração 0031 — trabalho de fase futura). Por ora a Etapa 7 entrega a parte que **não precisa** do input original: listar, mostrar estágios, cancelar, criar. Regra "degradação declarada > substituição silenciosa" (memory 2026-08-03) — a UI não finge que Retomar funciona. Verificações: `npm run build` verde (56 modules, era 52, +4; CSS 13.57 kB, era 10.22, +3.35; JS 201.74 kB, era 191.62, +10.12), `tsc --noEmit` verde, `check-docs.mjs` verde, `check-core-purity.ps1` verde. `cargo fmt` + `cargo clippy` (no `-p frederico-desktop`) verdes. **Fase 6 com 6/6 + Etapa 7 (UI/Polish) fechadas** — ADR-0028 §D1 (5 critérios) 5/5 fechados. **Próxima fase** depende de priorização do user: Fase 7 (Modo desenvolvedor) ou pausa de feature work pra focar em polish/segurança. | crates/e2e/tests/e2e_portao_transicao_e2e.rs::run_executor_rejects_invalid_transition_through_orchestrator, crates/e2e/tests/e2e_portao_transicao_e2e.rs::run_event_seq_monotonic_through_orchestrator, crates/e2e/tests/e2e_portao_transicao_e2e.rs::valid_transition_persists_in_run_event_journal, crates/e2e/tests/e2e_portao_transicao_e2e.rs::recovery_loads_state_from_run_event_journal, crates/e2e/tests/e2e_specialist_registry_e2e.rs::registry_loads_specialists_from_catalog, crates/e2e/tests/e2e_specialist_registry_e2e.rs::specialist_unknown_id_returns_structured_error, crates/e2e/tests/e2e_permission_loader_e2e.rs::permission_set_inherited_from_assistant_project_user, crates/e2e/tests/e2e_permission_loader_e2e.rs::effective_permission_set_is_subset_of_parent, crates/e2e/tests/e2e_subagent_e2e.rs::subagent_runs_with_reduced_permissions, crates/e2e/tests/e2e_subagent_e2e.rs::subagent_inherits_cancellation_token, crates/e2e/tests/e2e_subagent_e2e.rs::subagent_budget_discounted_from_parent_in_real_path, crates/e2e/tests/e2e_subagent_e2e.rs::subagent_explosion_cap_8_rejects_ninth, crates/e2e/tests/e2e_subagent_e2e.rs::subagent_depth_cap_2_rejects_grandchild, crates/e2e/tests/e2e_subagent_e2e.rs::subagent_budget_sum_never_exceeds_parent, crates/e2e/tests/e2e_pipeline_sequencial_e2e.rs::pipeline_two_stages_passes_artifact, crates/e2e/tests/e2e_pipeline_sequencial_e2e.rs::pipeline_survives_app_restart, crates/e2e/tests/e2e_pipeline_sequencial_e2e.rs::pipeline_skips_stage_when_input_artifact_unchanged, crates/e2e/tests/e2e_pipeline_sequencial_e2e.rs::pipeline_stage_cost_tracked, crates/e2e/tests/e2e_pipeline_sequencial_e2e.rs::pipeline_cancel_propagates_to_current_stage_and_skips_remaining, crates/e2e/tests/e2e_pipeline_sequencial_e2e_orchestrator.rs::pipeline_two_stages_executes_via_orchestrator, crates/e2e/tests/e2e_pipeline_sequencial_e2e_orchestrator.rs::cancel_pipeline_not_found_returns_structured_error, crates/e2e/tests/e2e_pipeline_sequencial_e2e_orchestrator.rs::start_pipeline_empty_stages_returns_error, crates/e2e/tests/e2e_pipeline_sequencial_e2e_orchestrator.rs::start_pipeline_unknown_provider_fails_at_stage, crates/e2e/tests/e2e_pipeline_sequencial_e2e_orchestrator.rs::pipeline_error_display_includes_context, crates/e2e/tests/e2e_pipeline_sequencial_e2e_orchestrator.rs::pipeline_d6_reuso_does_not_panic_when_no_reusable_stage, crates/e2e/tests/e2e_pipeline_sequencial_e2e_orchestrator.rs::list_resumable_returns_only_running_pipelines | cargo test --workspace, cargo test --workspace, cargo test --workspace, cargo test --workspace, cargo test --workspace, cargo test --workspace, cargo test --workspace, cargo test --workspace, cargo test --workspace, cargo test --workspace, cargo test --workspace, cargo test --workspace, cargo test --workspace, cargo test --workspace, cargo test --workspace, cargo test --workspace, cargo test --workspace, cargo test --workspace, cargo test --workspace, cargo test --workspace, cargo test --workspace, cargo test --workspace, cargo test --workspace, cargo test --workspace, cargo test --workspace, cargo test --workspace |
| 7 | Execução isolada (Modo Desenvolvedor: núcleo) | em andamento | **Etapa 1 (planejamento) fechada no PR `fase-7-etapa-1-planejamento`** (2026-08-08). 6 ADRs novos (0031 isolation model, 0032 scope reduction — Git/GitHub sai, 0033 network policy, 0034 write/exec approval, 0035 file ops overwrite, 0036 SecurityJailResolver — carry-over da Fase de Ligação). 2 specs novos ([`docs/architecture/runtimes-architecture.md`](architecture/runtimes-architecture.md) com Python+Node portáteis; [`docs/architecture/exec-tools-specification.md`](architecture/exec-tools-specification.md) com `exec.python`/`exec.node`/`exec.shell`). [`docs/architecture/windows-sandbox-design.md`](architecture/windows-sandbox-design.md) aprofundado do stub para spec completo (3 camadas combinadas: Jail + Job Object + Restricted Token + env zeroed; AppContainer adiado para Fase 8+; mapa de E2E planejado por etapa com 12 testes de negação nomeados). 4 specs atualizados: [`docs/architecture/development-roadmap.md`](architecture/development-roadmap.md) (Fase 7 vira só execução isolada, Fase 8 absorve Git/GitHub/diff/projetos/checkpoints); [`docs/architecture/security-threat-model.md`](architecture/security-threat-model.md) (ganha §"O que o sandbox NÃO protege" com 10 cenários honestos; cobertura planejada da Fase 7 com 7 testes para I1, I2, I3, D1, E1, E3, P1); [`docs/architecture/tool-registry-specification.md`](architecture/tool-registry-specification.md) (§"Status por ferramenta da Fase 7" lista 6 ferramentas planejadas com ADR e etapa). [`docs/releases/fase-7/README.md`](releases/fase-7/README.md) (índice de narrativas + tabela de etapas + carry-over da Fase de Ligação).

**Etapa 2 (Primitivas do sandbox) fechada no PR `fase-7-etapa-2-primitivas-sandbox`** (2026-08-08). 4 primitivas Rust em `crates/security/src/`:
- `env_filter.rs` (434 linhas, 8 tests) — `EnvAllowlist` com subenum `REQUIRED`/`ALLOWED`/`DENIED`, `EnvFilter::apply` remove DENIED in-place, default seguro `secure_default()`.
- `windows/job_object.rs` (424 linhas, 3 tests) — `JobObject` com `KILL_ON_JOB_CLOSE` + limites de memória (2 GB/processo, 4 GB/árvore), `Drop` fecha handle e cascateia terminação pra árvore inteira via `KILL_ON_JOB_CLOSE`. **Sem** `JOB_OBJECT_LIMIT_BREAKAWAY_OK` (default do Windows já garante que netos herdam o Job; o flag faria o oposto).
- `windows/restricted_token.rs` (326 linhas, 3 tests) — `RestrictedToken::from_current_process` faz `CreateRestrictedToken` dropando 6 privilégios elevados (SeDebug, SeBackup, SeRestore, SeTakeOwnership, SeLoadDriver, SeShutdown). Marcado `#[allow(dead_code)]` no v1 — a Etapa 4 pluga no `CreateProcessW` via `CommandExt::as_user`.
- `jail.rs` (593 linhas, 5 tests) — `SecurityJailResolver` orquestrador: cria o root Job + Token + EnvFilter em `new()`, atribui cada spawn ao Job via `assign_pid`. **Sem** chamada a `env_clear()` ou `envs()` (Windows quebra com `ERROR_INVALID_PARAMETER` 87 em env block grande) — degradação declarada: env do pai é herdado na v1; a Etapa 4 corrige com `CreateProcessW` raw.

**Dois testes de integração** em `crates/security/tests/tree_kill.rs` (2 tests, ambos passam):
- `fase5_etapa2a_incomplete_kill_parent_does_not_kill_grandchild` — **controle negativo** (regressão da Fase 5 Etapa 2.A PR #22): sem Job Object, `Child::kill()` no pai **NÃO** mata o neto. Prova que a Etapa 2.A da Fase 5 era quebrada — é exatamente isso que a Etapa 2 corrige.
- `job_object_kills_tree_on_resolver_drop` — **controle positivo**: com `SecurityJailResolver`, drop do resolver (que fecha o Job) mata a árvore via `KILL_ON_JOB_CLOSE`. Prova que a fix funciona.

**Conceitos separados** (correção do `main.rs` da Tauri): `SecurityJailResolver` (Etapa 2) é **complementar** ao `FileSystemJailResolver` da Fase 6 Etapa 1, não substituto. O primeiro orquestra spawn isolado (Job + Token + Env); o segundo resolve o `Jail` (path safety) por `ConversationId`. A integração com o `RunExecutor` da Fase 3 entra na **Etapa 4** (executa `exec.python`/`exec.node` sob o `SecurityJailResolver`). Suíte workspace verde (29 unit + 2 tree_kill + 6 windows_credential_store = 37 tests na `frederico-security`).

**Pendência técnica da v1**: `tokio::process::Command::envs()` falha com `ERROR_INVALID_PARAMETER` (87) em Windows quando o env block é grande; a v1 herda o env do parent automaticamente (env do pai é o que o filho vê, sem o filtro DENIED aplicado por re-injeção). A Etapa 4 corrige com `CreateProcessW` raw, passando o env block construído manualmente. Não é security hole — é limitação da v1 declarada (mesma regra "degradação declarada > substituição silenciosa" de 2026-08-03).

**Etapa 3 (Runtimes embutidos) fechada no PR `fase-7-etapa-3-runtimes-embutidos`** (2026-08-08). Novo crate `frederico-runtimes` (16º membro do workspace, `unsafe_code = "forbid"`) com 4 módulos Rust + 5 testes de regressão:
- `runtime.rs` (~300 linhas) — `Runtime` trait + `RuntimeId` (newtype com `parse_strict` anti-path-injection) + helpers. Cada runtime concreto declara `source_url` + `expected_sha256` + `expected_archive_size` (defesa em profundidade contra MITM de download).
- `python.rs` (~330 linhas, 0 unit) — `PythonRuntime` para Python 3.12.4 (CPython Windows embeddable package). **SHA-256 verificado em 2026-08-08** via download real (`15fea3c9367653a85086fe37216b4d1a1c78688fa5e1587e1db0b0f658856564`), size `11_065_736` bytes. Source URL pinned em `https://www.python.org/ftp/python/3.12.4/python-3.12.4-embed-amd64.zip`.
- `node.rs` (~250 linhas, 0 unit) — `NodeRuntime` para Node 20.16.0 (build portable). **SHA-256 verificado em 2026-08-08** (`4e88373ac5ae859ad4d50cc3c5fa86eb3178d089b72e64c4dbe6eeac5d7b5979`), size `29_553_046` bytes. Layout diferente: zip cria subpasta `node-v20.16.0-win-x64/`.
- `registry.rs` (~280 linhas) — `RuntimeRegistry` com `bootstrap_all` async + `BootstrapReport` (bootstrapped/cached/failed + total_duration + bytes_downloaded) + `cleanup_old_versions` (mantém `keep_n` mais recentes). `RuntimeConfig` resolve `install_root` via `directories::ProjectDirs` (Windows: `%LOCALAPPDATA%\FredericoAIStudio\runtimes\`).
- `bootstrap.rs` (~190 linhas) — download com retry exponencial 1s/2s/4s + extração de zip (Rust-pura, sem `unsafe`) + SHA-256 (`sha2` 0.10) + validação `<runtime> --version`.
- `manifest.rs` (~120 linhas) — schema JSON do `manifest.json` (read/write atômico via tmp+rename).

**5 testes de integração** em `crates/runtimes/tests/` (todos passam):
- `python_env_vars_do_not_include_user_paths` (controle **negativo** — regra do user 2026-08-08) — PATH injetado não contém hijack patterns (`Microsoft\WindowsApps`, `AppData\Local\Programs`, etc).
- `node_env_vars_do_not_include_user_paths` (controle **negativo**) — mesma defesa para Node.
- `bootstrap_twice_is_noop` (controle positivo) — 2ª chamada >5x mais rápida que a 1ª (cache hit, sem rede).
- `offline_returns_error_for_missing_runtime` (controle positivo) — `allow_download=false` + cache vazio = `Err(OfflineRequired)`, não panic.
- `corrupted_manifest_triggers_redownload` (controle positivo) — SHA-256 mismatch deleta + re-download.

**Suíte workspace verde**: 5/5 tests na `frederico-runtimes`. `cargo fmt --check` exit 0; `cargo clippy -p frederico-runtimes --all-targets -- -D warnings` exit 0; `node scripts/check-docs.mjs` exit 0 (com `docs/modules/runtimes.md` novo — §1.4 da REGRAS); `pwsh scripts/check-e2e-gate.ps1` exit 0 (Etapa 3 não altera o gate — testes em `crates/runtimes/tests/` não estão no escopo do gate que cobre `crates/e2e/tests/`); `pwsh scripts/check-core-purity.ps1` exit 0.

**Conceitos separados (cross-cutting)**: `frederico-runtimes` é **independente** — não importa `frederico-security` (poderiam ser irmãos sob `frederico-app`; a integração dos dois fica na Etapa 4, quando `exec.python`/`exec.node` rodam sob o `SecurityJailResolver` consumindo o `Runtime::env_vars`).

**Achados durante a implementação** (registrados pra Etapa 4 e Etapa 5):
- **Teste "negação" do PATH injetado**: o pattern `C:\Users\<name>` é **muito amplo** (casa com o tempdir do `tempfile::TempDir` que vive em `%LOCALAPPDATA%\Temp\<user>\`). Refinei pra padrões cirúrgicos: `Microsoft\WindowsApps`, `AppData\Local\Programs`, `AppData\Roaming\Python`, etc. — não bloqueia tempdir legítimo do user.
- **Bootstrap offline**: o `bootstrap_if_needed` precisa checar `allow_download=false` **antes** de tentar o download (mesmo com cache vazio). Sem essa checagem, ele tenta o download e falha com `ExtractFailed` em vez de `OfflineRequired` (recuperable).

Próxima: **Etapa 4 (exec.python / exec.node no ToolRegistry)** — `FilesExecTool::Python` + `FilesExecTool::Node` consumindo `RuntimeRegistry` + `SecurityJailResolver`; aprovações por escopo (ADR-0034); audit com comando exato | env filter `tokio::process::Command::envs()` não aplicado por re-injeção (Etapa 4 corrige com `CreateProcessW` raw); `RestrictedToken` construído mas não aplicado via spawn (Etapa 4 pluga via `CommandExt::as_user`); sem pipes pra stdout/stderr collection na v1 (Etapa 4 pluga); sem wall-clock timeout enforcement na v1 (caller usa `tokio::time::timeout`); sem `JOB_OBJECT_LIMIT_BREAKAWAY_OK` (default do Windows é mais seguro); SHA-256 e tamanho do Python/Node pinned como `const` no source (v2 migra para `runtime.toml` + migration SQL); sem `mirror_url` ativo (válvula de escape para mirror corporativo) | `crates/security/src/env_filter.rs::EnvFilter::apply`, `crates/security/src/windows/job_object.rs::JobObject::new`, `crates/security/src/windows/job_object.rs::JobObject::drop`, `crates/security/src/windows/restricted_token.rs::RestrictedToken::from_current_process`, `crates/security/src/jail.rs::SecurityJailResolver::new`, `crates/security/src/jail.rs::SecurityJailResolver::spawn`, `crates/security/tests/tree_kill.rs::fase5_etapa2a_incomplete_kill_parent_does_not_kill_grandchild`, `crates/security/tests/tree_kill.rs::job_object_kills_tree_on_resolver_drop`, `crates/runtimes/src/runtime.rs::Runtime::bootstrap_if_needed`, `crates/runtimes/src/registry.rs::RuntimeRegistry::bootstrap_all`, `crates/runtimes/src/python.rs::PythonRuntime::bootstrap_if_needed`, `crates/runtimes/src/node.rs::NodeRuntime::bootstrap_if_needed`, `crates/runtimes/tests/python_bootstrap.rs::python_env_vars_do_not_include_user_paths`, `crates/runtimes/tests/node_bootstrap.rs::node_env_vars_do_not_include_user_paths`, `crates/runtimes/tests/bootstrap_idempotent.rs::bootstrap_twice_is_noop`, `crates/runtimes/tests/bootstrap_offline.rs::offline_returns_error_for_missing_runtime`, `crates/runtimes/tests/manifest_corruption.rs::corrupted_manifest_triggers_redownload` | cargo test -p frederico-security, cargo test -p frederico-runtimes |
| 8 | Modo Desenvolvedor integrado | não iniciada | — | depende de 3 + 4 + 6 + 7; absorve Git/GitHub/diff/projetos/checkpoints/copiloto conforme [ADR-0032](decisions/0032-fase-7-scope-reduction.md) | - | - |
| 9 | Produção | não iniciada | — | depende de todas | - | - |
