{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissões padrão da Fase 1. Apenas IPC e lifecycle são necessários; tools e filesystem entram nas fases seguintes.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
